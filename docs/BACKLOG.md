# Backlog

Kind: Living.

The open acceptance items after the 2026-09-16 integration. Details and history for each are in DECISIONS.md under the same O number.

| ID | Requested outcome | State | Evidence or next step |
|---|---|---|---|
| B1 (O16) | A real new builder completes the M5 rehearsal | blocked on a participant | docs/rehearsals/NEW_BUILDER.md |
| B2 (O9) | Codex lifecycle hooks observed in real sessions | open: a signed-in Codex 0.156.0 session ran in a prepared repository on 2026-09-22; AGENTS.md and the skills reached the model and no Skilliton hook ran, while that version still read the plugin's `hooks/hooks.json` (evidence/live/2026-09-22-codex-session.md) | test whether a hook that a person has trusted, shipped in a plugin, runs in Codex 0.156.0 |
| B3 (O8) | A model session and automatic updates observed in a clean Claude Code configuration | blocked on one login in a fresh `CLAUDE_CONFIG_DIR` | company release rehearsal already covers install, update and verify there |
| B5 (O6, O7) | The session start and the guardrails confirmation prompt seen in an interactive session | half done: the session start reached the model in an interactive session of the VS Code extension on 2026-09-22 (evidence/live/2026-09-22-vs-code-extension-hooks.md; O6 closed); the confirmation prompt a person sees still needs a person at the keyboard | headless behavior measured (evidence/rehearsals/2026-09-16-live-clients); test 2 of docs/OWNER_TESTS.md |
| B8 (O10, O19, O21, O22) | Smaller follow-ups: guardrails allowlist for env files, verify's tag policy, guardrails client detection, secrets tiering review | open; O21 needs the Codex run in B2 and O22 a real application | DECISIONS.md open items (O18 and O20 closed; docs/BACKLOG_ARCHIVE.md) |
| B9 (O2, O3) | Usage reproduction before any savings claim | blocked on the owner's original scope and cutoff | DECISIONS.md O2 |
| B36 | The compaction window: with a large model and subagent-heavy work a 200000-token window is consumed fast and the owner could not get work done under it; find what actually keeps a session usable (a larger window in the team settings, a subagent budget, handoffs at natural boundaries) and measure it with the meter | half done: the template moved to 600000 by the owner's choice on 2026-09-18, unmeasured (decision 2026-09-18-the-team-compaction-window-is-600000-own-e7ee); no per-subagent window exists in the documented agent frontmatter; a headless session measured the mechanism at 100000 (rehearsal L6, 2026-09-18) and found that the client ends a session that refills the window within 3 turns of a compaction three times over, so a window that is small against the tool outputs a session takes is not merely compacted through, it is ended; the measurement of 600000 through the meter is still open (area 04 batch 01); and on 2026-09-20 the build session in this repository recorded 17 automatic compactions, none of them near 600000 (just short of 1M until 2026-09-17, then steadily 164741 to 179051 from 2026-09-18T21:54 onward, including after the setting was committed), so whether a project's value is read at all is now open too, and the test is the first automatic compaction of a session started after the setting (`evidence/live/2026-09-20-vs-code-extension-session.md` section 5). That test ran on 2026-09-22: two automatic compactions came at 568433 and 569293 tokens, under the project's 600000 and far from the user setting, so the project value is read (evidence/live/2026-09-22-compaction-window-measured.md; O25 closed). Why the sessions of 2026-09-18 to 2026-09-21 compacted near 170000 is still not explained | `templates/project-settings.json`; `scripts/skilliton.test.mjs` pins it |
| B14 | A live "one company, two developers" demonstration built on the existing rehearsals and demo, showing only the Phase 3 work that passed its checks | the kit is built and runs: `scripts/demo-day.mjs` (the five steps end to end on disposable folders, in about a minute), `scripts/fixtures/demo-app/` (the sample application) and docs/DEMO.md (the talk track, with what not to claim). Left: rehearse it with the real Claude Code, and give it | docs/DEMO.md; docs/PHASE-3.md "The demonstration as planned" |
| B15 (M14) | A read-only team view for a lead, generated locally from project records, verify results and audit findings | open, after the demonstration | PLAN.md M14; docs/PHASE-3.md |
| B16 (M14) | A small measured comparison against the plain tool (tokens cross-checked, time, interventions, rework, planted flaws caught, resume accuracy) before any time or cost statement | half done: the first run is filed (evidence/comparison/2026-09-22/SUMMARY.md, under docs/COMPARISON_PROTOCOL.md); the cross-check against the client's Usage screen remains, and it is the owner's (area 10 batch 02 item 2) | PLAN.md M14; DECISIONS.md O2 |
| B21 (M11) | Routines while working: dispatch suggestion, checkpoint snapshot around compaction, maintain suggestion, each measured live on Claude Code | open: the dispatch and maintain suggestions were seen in live headless sessions on 2026-09-22 (evidence/live/2026-09-22-dispatch-automation-live.md, evidence/live/2026-09-22-maintain-automation-live.md); left: the checkpoint snapshot written before compaction, and a per-project switch for the dispatch suggestion | PLAN.md M11 |
| B22 (M12) | Enrollment through a company's device management: profile, bundle, first-login setup, detection and offboarding scripts, rings | open; the Linux half of the order 2 spike is measured (evidence/rehearsals/2026-09-17-enrollment: the drop-in alone is active from the third session start, with a first-login install from the first); the macOS half needs a separate Mac or a macOS virtual machine; a real Intune or Jamf tenant needs the owner | PLAN.md M12; docs/PHASE-3.md riskiest assumption |
| B23 (M13) | Any AI coding tool: an adapter and a measured CLIENTS.md column per tool | open; order 5 is Cursor, on the owner's account (O24, decided); live sessions need the owner's Cursor login on this machine; other tools ranked after it. The Claude Code VS Code extension has a measured column as of 2026-09-20 (docs/CLIENTS.md), measured again on 2026-09-22 in a session whose plugins were loaded at its start (evidence/live/2026-09-22-vs-code-extension-hooks.md) | PLAN.md M13; docs/PHASE-3.md Tool support |
| B24 | Skilliton helps each developer's own skills grow over time, measured before it is claimed (owner direction, 2026-09-17) | proposed, not yet a milestone; needs a design and a measure | docs/PHASE-3.md "Direction the owner added" |
| B29 (M12) | First login ends VERIFIED under a real endpoint-security policy, with the preflight check naming an item the policy blocks on purpose | half done: the preflight check is proved against blocks made on purpose on a real machine (`scripts/preflight.test.mjs`, 10 cases); a run under a real product stays not measured; the owner has no endpoint-security product (2026-09-17), so the preflight is proved against blocks created on purpose on real machines and a run under a real product stays not measured (decision 2026-09-17-windows-is-in-scope-and-endpoint-securit-f76a) | docs/PHASE-3.md owner inputs |
| B30 (M12, M13) | Windows supported and rehearsed | the approach is decided (2026-09-17, owner): Git for Windows is required, one implementation of every hook, no PowerShell twin; every hook now pins `"shell": "bash"` and the preflight check looks for Git Bash there. Built 2026-09-19: join writes a `skilliton.cmd` launcher on Windows for PowerShell and the Command Prompt, content and undo pinned by `scripts/join.test.mjs` with the platform injected. Not rehearsed: the owner runs the first Windows session, including that launcher, and reports what happens. Ported 2026-09-23 on a hosted runner: preflight, prepare, status, every session hook and the guard's decisions pass there (evidence/live/windows/2026-09-23-hosted-runner-port.md); B79 is closed and B80 stands before support, and a session on a real Windows machine is still the owner's | decision 2026-09-17-windows-is-supported-through-git-for-win-cb9e; docs/IT-ALLOWLIST.md section 8 |
| B32 | Starter skill packs beyond the process skills (for example for common web stacks, data privacy and form handling), each with eval cases run with and without it, so a company begins with engineering skills and writes only what is its own | proposed from the owner's "where is the value if we still write the skills" question (2026-09-17); not yet a milestone; relates to B17 | docs/POSITIONING.md |
| B58 | One test convention in scripts/: every check under `node --test`, and the three checkers that are linters rather than tests named as such | deferred 2026-09-24 until no lane is open (docs/decisions/2026-09-24-three-backlog-rows-wait-while-the-batch-1492.md); open, after 1.0.0 on purpose: 15 files use `node:test` and 16 are bare scripts, and changing them renames CI steps and test files for no change in behavior the day before a release. Since 2026-09-22 the check list lives in `.github/workflows/checks.yml` alone (B43), so the rename touches one list, not two | `.github/workflows/checks.yml` |
| B66 | Comparison task B measures the guard, not the model noticing a broken fixture | open, no owner input needed; in the 2026-09-22 run the colleague's commit shared no history with the local branch, so the model saw the danger in both setups | Build task B's remote from the local clone so the histories are related, then rerun B |
| B67 | The guardrails hook's command parsing is reviewable: the tokenizer moves from 1,400 lines of Bash and awk to Node, which the runtime already needs, behind a thin Bash shim | measured 2026-09-24 and not merged: docs/decisions/2026-09-24-the-guard-s-tokenizer-stays-in-awk-the-n-44a4.md; open, proposed by the 2026-09-22 hiring-panel dry run and the code-quality review; large, after 1.1.0 | A lane with the existing 793 guard checks as its acceptance test |
| B68 | Line length and function length are held by the lint like file length is, and the longest functions are split (doctor, checkpoint, migrate, plan0003, join) | deferred 2026-09-24: a function is split when a change needs it (docs/decisions/2026-09-24-three-backlog-rows-wait-while-the-batch-1492.md); open, from the 2026-09-22 reviews: 938 plugin lines over 160 characters, one of 5,478 | Add the rule with pinned exceptions that may only shrink, then split one function per commit |
| B72 | Evidence a repository already has (its CI secret scan, dependency audit, test reports) is ingested as artifacts | deferred 2026-09-24 while the batch makes existing promises dependable (docs/decisions/2026-09-24-three-backlog-rows-wait-while-the-batch-1492.md); open, from the field report (N46) | an ingest collector per format, with the artifact fingerprinted like any other |
| B80 | The guard finishes a command of several hundred KB inside its timeout on Windows | open: 59 s for an 870 KB heredoc and 14 s for a 400 KB line on a hosted runner (evidence/live/windows/2026-09-23-hosted-runner-port.md); ordinary commands pass | the Node tokenizer (B67), or a size cut that asks instead of parsing |
| B85 | B80's Windows measurement is re-run with the 64 KB cap | open, 2026-09-23: guardrails 0.10.0 asks at once for a command over 64 KB, so the 59 s for an 870 KB heredoc on Windows no longer happens; the row stays until a Windows run confirms it | evidence/live/windows/ |
| B89 | `dispatch close` and the usage ledger report the cost of a lane run as an agent | open, measured 2026-09-24: closing LANES-8, 8b and 8c printed 0 requests for all seven lanes, because a lane run as a `workflow:lane` agent is recorded in the main window's transcript folder (its subagent transcripts), not under the lane folder the close reads | attribute a lane agent's transcript to its lane by the lane folder its brief and commands name (its recorded working directory is the main window's, measured in this batch), and test it with a fixture main-window folder holding one |

Give each item a stable ID and link its task and decisions. Move a completed item to `docs/BACKLOG_ARCHIVE.md` with its closure date and evidence.

<!-- skilliton:security-findings:start -->
Security findings: 6 open, listed in [docs/SECURITY\_FINDINGS.md](SECURITY_FINDINGS.md) (generated by skilliton security findings; do not edit between these markers).
<!-- skilliton:security-findings:end -->
