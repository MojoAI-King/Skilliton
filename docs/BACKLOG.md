# Backlog

Kind: Living.

The open acceptance items after the 2026-09-16 integration. Details and history for each are in DECISIONS.md under the same O number.

| ID | Requested outcome | State | Evidence or next step |
|---|---|---|---|
| B1 (O16) | A real new builder completes the M5 rehearsal | blocked on a participant | docs/rehearsals/NEW_BUILDER.md |
| B2 (O9) | Codex lifecycle hooks observed in real sessions | blocked on a logged-in isolated Codex home | `CODEX_HOME=<folder> codex login`, then `node scripts/rehearsals/live-clients.mjs --codex-home <folder>` |
| B3 (O8) | A model session and automatic updates observed in a clean Claude Code configuration | blocked on one login in a fresh `CLAUDE_CONFIG_DIR` | company release rehearsal already covers install, update and verify there |
| B4 (O15) | The GitHub delivery adapter rehearsed on a hosted repository with branch protection | blocked on approval to create a throwaway repository | docs/DELIVERY.md |
| B5 (O6, O7) | The session start and the guardrails confirmation prompt seen in an interactive session | needs a person at the keyboard | headless behavior measured (evidence/rehearsals/2026-09-16-live-clients) |
| B6 (O17) | A signed release of this repository | needs the owner's signing key and version choice | docs/RELEASING.md |
| B7 | Applicability decisions for this repository's own security register | needs the owner | `skilliton security status`, then `skilliton security applicability ... --decided-by <owner> --apply` |
| B8 (O10, O19, O21, O22) | Smaller follow-ups: guardrails allowlist for env files, verify's tag policy, guardrails client detection, secrets tiering review | open; O21 needs the Codex run in B2 and O22 a real application | DECISIONS.md open items (O18 and O20 closed; docs/BACKLOG_ARCHIVE.md) |
| B9 (O2, O3) | Usage reproduction before any savings claim | blocked on the owner's original scope and cutoff | DECISIONS.md O2 |
| B13 (M8) | A never-prepared repository is offered preparation at session start, and a delivery policy is drafted from its detected test commands | open; first increment built 2026-09-18 (the offer); second increment built 2026-09-19 (workflow 0.10.0): apply shows the plan first, drafts dispatch.laneTestCommand, laneRoot and hotspots and a delivery policy draft in `.skilliton/delivery.draft.json` that `delivery confirm` moves, and the first task takes the user's words through `task start --request`; rehearsed on a node, a python and a go repository (`evidence/rehearsals/2026-09-19-projects/`); the live unprepared session remains (owner pass, docs/REPORT_CARD.md) | PLAN.md section 7 M8; `scripts/lifecycle.test.mjs` (the offer line) |
| B35 | Maintenance on a rolling basis: the records (checkpoints, decisions, lessons, indexes, handoff) are written as the work happens, so the end-of-session `/workflow:maintain` shrinks from a 5 to 10 minute block to a short reconciliation | half done 2026-09-19: the mechanical parts (task handoff, RESUME HERE block, indexes) are written by `skilliton checkpoint` (workflow 0.9.0, decision 2026-09-19-records-are-written-at-checkpoint-time-m-554d); the maintain skill reconciles; the minutes are the owner's to report (05-05) | docs/areas/01-autopilot-loop/batch-02-rolling-maintenance.md; before and after counts pending |
| B36 | The compaction window: with a large model and subagent-heavy work a 200000-token window is consumed fast and the owner could not get work done under it; find what actually keeps a session usable (a larger window in the team settings, a subagent budget, handoffs at natural boundaries) and measure it with the meter | half done: the template moved to 600000 by the owner's choice on 2026-09-18, unmeasured (decision 2026-09-18-the-team-compaction-window-is-600000-own-e7ee); no per-subagent window exists in the documented agent frontmatter; a headless session measured the mechanism at 100000 (rehearsal L6, 2026-09-18) and found that the client ends a session that refills the window within 3 turns of a compaction three times over, so a window that is small against the tool outputs a session takes is not merely compacted through, it is ended; the measurement of 600000 through the meter is still open (area 04 batch 01); and on 2026-09-20 the build session in this repository recorded 17 automatic compactions, none of them near 600000 (just short of 1M until 2026-09-17, then steadily 164741 to 179051 from 2026-09-18T21:54 onward, including after the setting was committed), so whether a project's value is read at all is now open too, and the test is the first automatic compaction of a session started after the setting (`evidence/live/2026-09-20-vs-code-extension-session.md` section 5) | `templates/project-settings.json`; `scripts/skilliton.test.mjs` pins it |
| B14 | A live "one company, two developers" demonstration built on the existing rehearsals and demo, showing only the Phase 3 work that passed its checks | the kit is built and runs: `scripts/demo-day.mjs` (the five steps end to end on disposable folders, in about a minute), `scripts/fixtures/demo-app/` (the sample application) and docs/DEMO.md (the talk track, with what not to claim). Left: rehearse it with the real Claude Code, and give it | docs/DEMO.md; docs/PHASE-3.md "The demonstration as planned" |
| B15 (M14) | A read-only team view for a lead, generated locally from project records, verify results and audit findings | open, after the demonstration | PLAN.md M14; docs/PHASE-3.md |
| B16 (M14) | A small measured comparison against the plain tool (tokens cross-checked, time, interventions, rework, planted flaws caught, resume accuracy) before any time or cost statement | open; paid runs need the owner's approval and a ceiling | PLAN.md M14; DECISIONS.md O2 |
| B17 | An optional code-quality pack of cleanup skills, each with eval cases run with and without it, following M10's pattern of deterministic checks plus a skill | proposed, and now unblocked: M10 landed on 2026-09-20 as `skilliton audit`, which is the deterministic half this item was waiting for. Restated 2026-09-20 with what wave 8 has to build first: **there are no cleanup skills to evaluate yet**, because `packs/` holds only process skills, so the first work is writing them, not running an eval. The eval spend is authorized (owner, 2026-09-20) and carries forward | docs/POSITIONING.md; the pattern to follow is `runtime/lib/audit.mjs` plus `scripts/audit.test.mjs`; the audit's own eval half (a skill helping a model act on findings) belongs to this item, not to batch 09-02 |
| B18 (M12) | Installs pinned to signed release tags, so a laptop cannot follow the marketplace branch past the approved release; the basis of release rings | open, part of M12; `ref` and `sha` pins are documented by Claude Code, not yet tested | docs/PHASE-3.md M12; docs/CLIENTS.md marketplace sources |
| B21 (M11) | Routines while working: dispatch suggestion, checkpoint snapshot around compaction, maintain suggestion, each measured live on Claude Code | open; order 4 | PLAN.md M11 |
| B22 (M12) | Enrollment through a company's device management: profile, bundle, first-login setup, detection and offboarding scripts, rings | open; the Linux half of the order 2 spike is measured (evidence/rehearsals/2026-09-17-enrollment: the drop-in alone is active from the third session start, with a first-login install from the first); the macOS half needs a separate Mac or a macOS virtual machine; a real Intune or Jamf tenant needs the owner | PLAN.md M12; docs/PHASE-3.md riskiest assumption |
| B23 (M13) | Any AI coding tool: an adapter and a measured CLIENTS.md column per tool | open; order 5 is Cursor, on the owner's account (O24, decided); live sessions need the owner's Cursor login on this machine; other tools ranked after it. The Claude Code VS Code extension has a measured column as of 2026-09-20 (docs/CLIENTS.md, 28 rows), with its plugin rows recorded as not settled because the plugins were installed 59 hours into that session | PLAN.md M13; docs/PHASE-3.md Tool support |
| B24 | Skilliton helps each developer's own skills grow over time, measured before it is claimed (owner direction, 2026-09-17) | proposed, not yet a milestone; needs a design and a measure | docs/PHASE-3.md "Direction the owner added" |
| B29 (M12) | First login ends VERIFIED under a real endpoint-security policy, with the preflight check naming an item the policy blocks on purpose | half done: the preflight check is proved against blocks made on purpose on a real machine (`scripts/preflight.test.mjs`, 10 cases); a run under a real product stays not measured; the owner has no endpoint-security product (2026-09-17), so the preflight is proved against blocks created on purpose on real machines and a run under a real product stays not measured (decision 2026-09-17-windows-is-in-scope-and-endpoint-securit-f76a) | docs/PHASE-3.md owner inputs |
| B30 (M12, M13) | Windows supported and rehearsed | the approach is decided (2026-09-17, owner): Git for Windows is required, one implementation of every hook, no PowerShell twin; every hook now pins `"shell": "bash"` and the preflight check looks for Git Bash there. Built 2026-09-19: join writes a `skilliton.cmd` launcher on Windows for PowerShell and the Command Prompt, content and undo pinned by `scripts/join.test.mjs` with the platform injected. Not rehearsed: the owner runs the first Windows session, including that launcher, and reports what happens | decision 2026-09-17-windows-is-supported-through-git-for-win-cb9e; docs/IT-ALLOWLIST.md section 8 |
| B31 (M12) | Install, update and verify from a private company repository with real GitHub credentials, including Claude Code's background updates | needs approval for a throwaway private repository | docs/COVERAGE.md releases; docs/PHASE-3.md owner inputs |
| B32 | Starter skill packs beyond the process skills (for example for common web stacks, data privacy and form handling), each with eval cases run with and without it, so a company begins with engineering skills and writes only what is its own | proposed from the owner's "where is the value if we still write the skills" question (2026-09-17); not yet a milestone; relates to B17 | docs/POSITIONING.md |
| B37 | One `OperationFailed` class, not two | open, no owner input needed; found by reading on 2026-09-20 while shipping dispatch, and not reproduced, so the impact is unverified. `runtime/lib/prepare.mjs` and `runtime/lib/lifecycle.mjs` each define a class of that name, and `guardCommand` catches only the lifecycle one | move the class into one module and have both import it; a test that makes prepare fail and reads the exit code would show whether the second class ever reaches a user |
| B38 | The first real dispatch: two lanes launched from their briefs in one session, each lane's peak context filed from the meter | open; deferred by the owner on 2026-09-20 to the end-of-build pass, with both instruments built that day (workflow 0.12.0 puts the launch line in the brief and commits each lane's task record; `scripts/token-cost.mjs` reports `peak_ctx` per bucket, and `--by-project` makes that per lane because each worktree is its own project folder). No dispatch has ever been run, so every lane behavior in area 07 is proved by tests and by nothing else | docs/REPORT_CARD.md owner pass; docs/areas/07-dispatch-worktrees/batch-02-lane-agents-bounded.md items 1 and 3 |
| B39 | A formatter, or a recorded decision that this repository has none | open, no owner input needed; batch 06-01 is titled lint and format and only the lint shipped on 2026-09-20 (decision 2026-09-20-a-dependency-free-in-repo-lint-and-a-siz-da3d). A dependency-free formatter is a much larger thing than a dependency-free lint, and the alternative is a devDependency, which is the footprint claim the same decision protects | its own batch under area 06, or a decision entry saying the repository stays unformatted and why |
| B40 | The subprocess teardown copied between the delivery gate and `skilliton gate` | open, no owner input needed; found on 2026-09-20 by `scripts/deadcode.mjs`, the only duplicate it still reports. `runtime/lib/delivery.mjs:505` and `runtime/lib/gate.mjs:114` each end a checked subprocess the same way: clear both timers, destroy stdout and stderr, flush whatever the partial buffers hold, and compute the elapsed seconds. The copy stays for now because the seven lines are all reads and writes of the caller's own locals (`settled`, `timer`, `grace`, `child`, `partial`, `began`), so a shared helper would take seven arguments to save seven lines and would leave two subprocess supervisors depending on a function whose job is to mutate their state. Its row is in `ALLOWED_DUPLICATE` in `scripts/deadcode.mjs` naming this id | the fix that would earn its keep is one shared subprocess supervisor for both callers, not one shared teardown; `scripts/gate.test.mjs` and `scripts/delivery.test.mjs` cover both sides today |
| B41 | One migration id pattern, not two that disagree | open, no owner input needed; found on 2026-09-20 while narrowing the runtime's exports. `runtime/lib/migrations.mjs:49` uses `/^\d{4}-[a-z0-9]+(?:-[a-z0-9]+)*$/` and `runtime/lib/release.mjs:24` uses `/^\d{4}-[a-z0-9][a-z0-9-]*$/`. Measured, they accept the same ids except for a doubled or a trailing hyphen: `0001-a--b` and `0001-a-` pass the release manifest check and fail the migration loader. No such id exists today, so nothing is broken; the risk is a release manifest that validates and a migration that then will not load | pick one pattern, export it from the module that owns migration ids, and have the other import it; a test with `0001-a--b` and `0001-a-` refused by both |
| B42 | The harness template's dispatch rule says **instructed** while a hook now gives the suggestion | open, no owner input needed; `packs/base/plugins/workflow/templates/harness.md` tells the assistant to use `/workflow:dispatch` at six or more items as an instructed behavior, and on 2026-09-20 a UserPromptSubmit hook started giving that note itself. The line should read enforced for Claude Code and instructed for Codex, in the shape the other rows use. It is deliberately not changed yet: editing that template makes migration 0100 pending in every prepared project including this one, so it rides with the next migration-bearing release. It is also the wrong claim to make until the live sighting in the owner pass proves the client delivers the event to a plugin hook | change the template's Starting a piece of work bullet in the same release that carries a migration, then `skilliton harness --apply` here |
| B43 | One committed list of this repository's offline checks, read by both the local run and CI, with a test that fails when they disagree | open, no owner input needed; found on 2026-09-20 while running the maintenance gates. The list lives in two hand-maintained places: `docs/MAINTAIN.md` step 2 as prose and `.github/workflows/checks.yml` as steps. Measured that day by extracting every `scripts/*.mjs` and `scripts/*.sh` path from both, they name the same 42 scripts, so they agree now and nothing holds them there. There is no runnable list at all: no root `package.json` by design, no Makefile, no `scripts/verify`, and no `.skilliton/delivery.json`, so `skilliton gate` in this repository has no checks of its own and needs `--cmd`. Each session retypes the suite into a scratchpad script, where a wrong name exits 1 and reads like a failing check and a missing name reads like a pass | lesson 2026-09-20-a-gate-list-typed-by-hand-from-prose-has-ce6a; the fix is one list both readers consume, which is also what would give this repository the delivery policy it ships to others |
| B44 | The git wrapper check's message names a remedy that branch does not have | open, no owner input needed; found on 2026-09-21 while diagnosing ten findings the check raised against the audit's own wrapper. `scripts/git-config.test.mjs:224` tells the reader to "write them where they can be read, or name the call in `WRAPPER_EXCEPTIONS` with its reason", and that branch never consults `WRAPPER_EXCEPTIONS`: only the two later branches at lines 237 and 243, for a scrubbed environment variable and for `userFacing: true`, read the list. A reader who follows the message adds an entry, the finding stays, and the unused entry is then reported by the check's own unused-exception report at line 259 | either make the branch honour the list, or take the second half of the sentence out; a case in the file's own self test that follows the message and proves it works |
| B45 | This repository's own 41 audit alarms carry an allow marker with a reason | open, no owner input needed; measured on 2026-09-21 by running `skilliton audit` over every file the repository has ever changed (475 files): **41 findings, none of them a real weakness**. 18 in `scripts/guardrails.test.sh` and 9 in `packs/base/plugins/guardrails/hooks/guard-bash.sh` are the guardrail's own refused patterns, 9 in `scripts/preflight.test.mjs` and 3 in `runtime/lib/trust.mjs` and `runtime/lib/preflight.mjs` are planted or scanned secret shapes, 1 is a plugin manifest sentence. Scoped the way the audit is meant to run, over the 47 files wave 7 changed, the same run reports 0 findings and 6 allowed lines. Nothing is blocked here today because this repository has no delivery policy of its own (B43), but a gated repository would reject a push touching any of those files | end each line with `skilliton-audit: allow <rule> <why>`, taking care with the lines inside template literals in `scripts/preflight.test.mjs`, then run the whole tree audit again and record the remaining count in docs/COVERAGE.md; lesson 2026-09-21-an-audit-finds-its-own-rules-written-dow-cc3f |
| B46 | The Claude Code version CI validates the plugins with is pinned, or its drift is recorded | open, no owner input needed; found 2026-09-21 and restated the same day after the local half of it was measured properly. `.github/workflows/checks.yml` installs the client with `npm install --global @anthropic-ai/claude-code`, unpinned, so the version that runs `claude plugin validate --strict` changes without notice: run 35559200451 printed **2.1.278**. docs/CLIENTS.md pins its matrix to 2.1.273, so the repository states one client version and validates against another. The local half of this row was wrong when first written: strict validation was never unavailable here, the suite script had invoked bare `claude`, which PATH resolves to 2.1.92 | pin the CI install to the version docs/CLIENTS.md names and raise both together, or keep it unpinned and have the step record the version it printed into the run's evidence; lesson 2026-09-21-a-not-run-on-this-machine-can-be-a-versi-1f6b |
| B11 | The stated Node.js 18 floor either run in CI or raised to a version CI runs, and supported operating systems stated as exercised | open, no owner input needed | docs/COVERAGE.md |

Give each item a stable ID and link its task and decisions. Move a completed item to `docs/BACKLOG_ARCHIVE.md` with its closure date and evidence.
