# Task: Lane usage

Kind: Living. Task record.

- **ID:** 2026-09-23-lane-usage-2ca2
- **State:** in-progress
- **Branch:** lane/usage-0924
- **Owner:** unassigned
- **Updated:** 2026-09-24T04:04:37.049Z

## Request

LANES.md, dispatched 2026-09-23: the items below are this lane's whole scope, and work that is not among them belongs to another lane.

## Acceptance criteria

- [x] N1. [FEATURE] The meter ships inside the workflow plugin and is the default: move scripts/token-cost.mjs to packs/base/plugins/workflow/runtime/meter/token-cost.mjs with the same contract (FROM_DAY TO_DAY, --project, --until, --by-project, --json, the PRICING table with its retrieval notes, peak_context) and its fixture test beside it as runtime/meter/token-cost.test.mjs with the fixtures the test needs under runtime/meter/fixtures/ (the runtime lint applies to every file there: under 600 lines, lines under 160 characters); scripts/token-cost.mjs becomes a shim that runs the plugin's meter with the same argv and exit code, so every document naming scripts/token-cost.mjs stays true, and scripts/token-cost.test.mjs runs the plugin's test including the --reference check (NOT RUN where the reference transcripts are absent, as today); lib/usage.mjs findMeter defaults to the plugin's meter, resolved from the runtime's own folder through import.meta.url, and uses a project's own scripts/token-cost.mjs only when it speaks the contract (probe it with --json over an empty window and require byScope, tz, incomplete and unpriced_models; refuse one that does not, naming what was missing and the plugin meter it will use instead); scripts/usage.test.mjs and scripts/token-cost-models.test.mjs follow the new paths; docs/IT-ALLOWLIST.md gains the program start and the writes this lane adds.
- [x] N2. [FEATURE] Batches come from maintain events and task closes, not merge commits: lib/usage.mjs batchRows: a batch ends at each maintain event in the journal (lib/journal.mjs readJournal; the journal is per machine under the Git dir) and at each task close (lib/tasks.mjs: a record whose State is merged, done-local, released or verified, at its Updated time), with merge commits kept as a third source; boundaries inside the same minute are one row naming all of them; the meter gains --since <ISO time> beside --until so a row's window is the instant span between two boundaries (the FROM_DAY TO_DAY form stays and the two compose); each row names its tasks, merges and maintain events; a repository with fast-forward merges and no journal still gets rows from its task closes; scripts/usage.test.mjs covers a fixture with fast-forward merges only (0 merge commits, 2 task closes, 1 maintain event) and asserts the rows and their boundaries.
- [x] N3. [TOUCH] The project is the default scope: without --project, usage passes the meter this repository's own transcript folder and its lane worktrees' folders, matched exactly and never by substring: the meter gains --project-dir <path> (repeatable), which turns a path into the Claude Code project folder name the way the client names it (verify the rule against a real folder name on this machine before writing it and say in the code comment what you verified; a real folder name is never written into the repository, a test uses a fixture name) and matches that name exactly; usage passes the repository root and every worktree under dispatch.laneRoot (from git worktree list --porcelain, filtered to the lane root) as --project-dir; --project keeps the substring form for a person; --all-projects widens to every project on the machine; the scorecard's second line says which scope was used; a case in scripts/usage.test.mjs with SKILLITON_PROJECTS pointing at a fixture holding two project folders, one of which must not be counted.
- [ ] N4. [FEATURE] A committed ledger keeps the rows: new lib/usage-ledger.mjs (under 600 lines) reads and appends .skilliton/usage/ledger.jsonl, one JSON object per line, kind batch (from, to, boundaries: tasks, merges, maintain events; requests, input, output, cache_read, cache_write_5m, cache_write_1h and peak_context per scope and per model; the meter's counters; incomplete and the unpriced models) and kind screen (N5); a row stores token counts and never a dollar figure and never a project folder name: cost is computed at display time from the meter's price table and the display names the table's retrieval date; skilliton maintain --apply (commands/maintain.mjs, lib/maintain.mjs) appends the batch row for the window since the last ledger row (or since the earliest transcript for the first row, and the row says so) before it records the maintain event, so the window ends at the maintain event and rows tile without a gap; the append is refused with nothing written when the meter's test has not just passed or a row for that window exists; when the journal is missing the row's start is the last ledger row, else the first task record, and the row says which; skilliton usage reads the ledger first and adds the live tail since the last row from the transcripts, marking which rows are committed and which are live; --ledger-only prints committed rows only, which is what another machine can read; scripts/usage-ledger.test.mjs: append, refuse a duplicate window, rows tile without a gap, a row carries no dollar figure and no folder name (assert on the file's text), and the display prices a fixture row from the table.
- [ ] N5. [FEATURE] The savings view, honest: skilliton usage screen --five-hour <percent> --weekly <percent> [--model-weekly <percent>] [--at <ISO time>] --apply appends a screen row (the Usage screen's readings as a person read them, whole numbers 0 to 100, refused otherwise; --at defaults to now); skilliton usage summary [--baseline <from>..<to>] prints one short summary: this period (since the last screen row, else the last seven days) against the baseline (docs/USAGE_BASELINE.md's window when it is chosen, else the earliest committed batch rows of the same length, and it says which), in tokens and estimated cost priced from the table; time per closed task from the task records (start to close: median and the longest, named); gate runs, reads refused and compactions in the period from scripts/token-direction.mjs's counting (reuse its functions, do not re-derive the meter's dedup); the screen readings beside the meter's trend, each with its date; the controlled comparison rows when evidence/comparison/<date>/results.jsonl exists in the repository, else the line "no controlled comparison filed"; and the verdict line, which prints the word saved only when the meter's per-comparable-batch trend and the screen readings both went down over the period, prints "not established" when they disagree or a reading is missing, and never prints a percentage or a dollar saving; RECONSTRUCTION_NOTE stays on every output; cases in scripts/usage.test.mjs: both down prints saved, meter down alone prints not established, no screen row prints not established and says a reading is missing, and no output line of any case carries a percent sign or a dollar sign on the same line as the word saved.
- [ ] N6. [TOUCH] Lane reports carry cost and peak context: skilliton dispatch close (N7) prints, for each lane branch contained in the integration branch, the lane worktree's transcript folder cost and peak context from the meter (--by-project with --project-dir <lane folder>) against dispatch's context bound (200000 by default, dispatch.contextCeiling when set), naming each lane that ran past it; the same lines are appended under a sixth heading "Cost and peak context" in the lane's LANE_REPORT.md when the file exists (never committed); packs/base/plugins/workflow/agents/lane.md and lib/dispatch-brief.mjs name that heading; the batch row N4 writes at maintain time carries the per-lane figures for lanes closed in the window (tokens and peak context, the lane name only, never the folder name).
- [ ] N7. [TOUCH] Closing a batch marks each merged lane's task record merged: skilliton dispatch close [--apply] finds every lane/* branch contained in the integration branch (git branch --merged), brings each lane's records back the way dispatch merge does (reuse it, do not copy it), closes the lane's task record with state merged when it is still in-progress through lib/tasks.mjs's close path with its backup, detaches the lane folder to the integration branch when the folder exists and holds no uncommitted work (refuse and name the folder otherwise; never delete a branch or a folder), and prints one line per lane; without --apply it previews; scripts/dispatch.test.mjs (or a new scripts/dispatch-close.test.mjs when the pinned file cannot grow) covers two merged lanes and one unmerged, the refusal on a dirty folder, and the task states after.

## Decisions

not yet written

## Checkpoints

### 2026-09-24T03:50:00.422Z

- **State:** N1 done: the meter ships in the workflow plugin (runtime/meter/token-cost.mjs, pricing.mjs, its test; fixtures in the plugin's fixtures/meter because the IT allow list check treats data under runtime/ as unvetted code); scripts/token-cost.mjs and its test are shims; usage defaults to the plugin meter and probes a project meter's contract
- **Evidence:** node scripts/token-cost.test.mjs --reference: fixture passed and the reference window reproduced 1919/439 requests; old and new meter give identical in-window JSON on a real two-day window; usage, models, direction, lint, lint-shape, allowlist, deadcode, docs, footprint, names, skilliton tests exit 0
- **Next:** N2: batch boundaries from maintain events and task closes, meter --since
- **Git:** lane/usage-0924 @ 70f92d9, 16 uncommitted

### 2026-09-24T04:00:06.429Z

- **State:** N2 done: batch rows end at maintain events, task closes (merged, done-local, released, verified) and merge commits; boundaries in one minute are one row; rows are instant spans the meter reads with the new --since (exclusive) and --until (inclusive), composing with FROM_DAY TO_DAY
- **Evidence:** meter fixture test covers --since and the composed form (mutation of the exclusive bound fails 8 checks); usage.test has the fast-forward fixture (0 merges, 2 task closes, 1 maintain event, 2 rows); lint, lint-shape, allowlist, deadcode exit 0; a real run on this checkout printed 2 rows from task closes
- **Next:** N3: --project-dir exact scope, default scope is the repository and its lane worktrees
- **Git:** lane/usage-0924 @ 3006093, 7 uncommitted

### 2026-09-24T04:04:37.049Z

- **State:** N3 done: the meter takes --project-dir (repeatable) and matches the Claude Code folder name exactly (every non-alphanumeric character one hyphen, not collapsed, from the real path; verified against this machine's folder names without writing any); usage passes the repository and its lane worktrees by default, --project stays a substring, --all-projects widens; the second scorecard line names the scope
- **Evidence:** meter test: exact vs substring fixture folders, absent folder counted and not incomplete; usage.test: SKILLITON_PROJECTS fixture with repository, lane and a sibling folder, default counts 2 of 3; real run on this checkout 5x faster than the substring scan; lint, lint-shape, allowlist, deadcode, footprint exit 0
- **Next:** N4: the committed ledger and the maintain step
- **Git:** lane/usage-0924 @ c5fbb45, 10 uncommitted

## Handoff

- **State:** N3 done: the meter takes --project-dir (repeatable) and matches the Claude Code folder name exactly (every non-alphanumeric character one hyphen, not collapsed, from the real path; verified against this machine's folder names without writing any); usage passes the repository and its lane worktrees by default, --project stays a substring, --all-projects widens; the second scorecard line names the scope. Evidence: meter test: exact vs substring fixture folders, absent folder counted and not incomplete; usage.test: SKILLITON_PROJECTS fixture with repository, lane and a sibling folder, default counts 2 of 3; real run on this checkout 5x faster than the substring scan; lint, lint-shape, allowlist, deadcode, footprint exit 0.
- **Next:** N4: the committed ledger and the maintain step
- **Blocked:** nothing
- **Watch out:** nothing known
