# Task: Lane guard

Kind: Living. Task record.

- **ID:** 2026-09-23-lane-guard-4301
- **State:** in-progress
- **Branch:** lane/guard-0924
- **Owner:** unassigned
- **Updated:** 2026-09-24T03:31:53.025Z

## Request

LANES.md, dispatched 2026-09-23: the items below are this lane's whole scope, and work that is not among them belongs to another lane.

## Acceptance criteria

- [ ] N8. [TOUCH] B84: the guard reads a sourced or executed script through its own rules instead of asking: guard-bash.sh, the ask rule from guardrails 0.10.0 for source <file>, . <file>, bash <file>, sh <file> and zsh <file>: when the file is inside the project, a regular file with no symbolic link at any step, under the 64 KB cap and readable, read its text and run it through the same deny and ask rules as a command, one level deep (a script that itself sources or executes another file still asks); allow when nothing fires; deny or ask with the inner reason and the file named when something does; keep asking for a file outside the project, a link, over the cap, unreadable, or one whose text the splitter cannot handle; a Python environment's bin/activate (a fixture under scripts/fixtures/ holding the real text venv writes) is the case that must allow; cases in scripts/guardrails-review2.test.sh (allow, deny inside, ask outside, link asks, oversize asks, nested source asks); plugin.json 0.10.0 to 0.11.0 in this commit; the SKILL.md limits list says what is read and what still asks.
- [ ] N9. [FEATURE, gated] B67: the tokenizer moves to Node behind a thin Bash shim: the splitting section of guard-bash.sh (lines 384 to 1221 at the base, "splitting the command", and the JSON reading at 220 to 383 if that simplifies it) becomes packs/base/plugins/guardrails/hooks/guard-parse.mjs, pure functions over the command text (tokens, operators, quotes, redirections, here-documents, substitutions), under 600 lines, every line under 160 characters, no dependencies, with a --self-test that feeds it the shapes the suites use; guard-bash.sh calls it once per hook run through the node the hook already finds, and when node is absent the hook asks with a reason and never allows; the rules stay in Bash and read the parse as before (the same variable names, or a mapping documented at the top); acceptance is every suite named above passing unchanged, with the slowest decision in the suites reported before and after (the hook's budget is 10 s and today's slowest is 153 ms); this item is committed only when every suite is green and the slowest decision stays under 500 ms: otherwise leave it uncommitted, keep N8's commit, and write under Skipped what stopped you and how far it got.

## Decisions

not yet written

## Checkpoints

## Handoff

- **State:** not yet written
- **Next:** not yet written
- **Blocked:** not yet written
- **Watch out:** not yet written
