# Handoff

Kind: Living.

## RESUME HERE

Written: 2026-09-16 18:35 EDT

- **State:** Milestones are unchanged since the integration: M1 verified locally; M2 verified on Claude Code (live sessions and the offline rehearsal); M3 verified at install level on Claude Code and Codex (18 of 18); M4 verified locally; M5 not started because it needs a person. Since then: the follow-ups that needed no owner input are closed (O4, O5, O13, O18, O20), published as 366468a with CI run 35153435426 green and every step read. Eval cases for the task and security skills are added and run (B10). Their first run found a skill fallback that contradicted the project instructions, unrequested work after a set-up request, a PATH lookup that failed on an unreadable entry, and a judge wording error; all are fixed (e87d2d1), and with the sonnet judge the task case scores 1.00 (difference 0.39) and the security case 1.00 (difference 0.33), evidence/e87d2d1.../. O12 is closed: the recorded eval command uses a sonnet judge. Versions: workflow 0.3.3, guardrails 0.2.0, context-hygiene 0.1.3. Published: 469ee8a passed CI run 35156760340, all 28 steps read. Maintenance since: docs/COVERAGE.md now holds what has and has not been exercised, consolidated from the lane reports and checked against code, tests and CI logs, which found that the stated Node.js 18 floor has never been run (B11); this repository's first decision and lesson entry files are in `docs/decisions/` and `docs/lessons/`, listed by the index sections now at the end of DECISIONS.md and docs/LESSONS.md, and new entries go there. This note is pushed with that maintenance; its CI result is read after the push. Installed: nothing from this marketplace in the owner's own Claude Code or Codex configuration; installs exist only in throwaway homes used by rehearsals and probes. Released: none. Verified: the rehearsal installs only.
- **Next:**
  1. Owner inputs, smallest first (docs/BACKLOG.md): `CODEX_HOME=<folder> codex login` once, then `node scripts/rehearsals/live-clients.mjs --claude <2.1.273 path> --codex <path> --codex-home <folder>` (B2); one login inside a fresh `CLAUDE_CONFIG_DIR` (B3); approval to create a private throwaway GitHub repository for the delivery adapter (B4); applicability decisions for this repository's 15 security controls (B7); a version and the owner's signing key for the first release (B6); a person for the M5 rehearsal (B1, docs/rehearsals/NEW_BUILDER.md).
  2. Without owner input: run the suite on Node.js 18 in CI or raise the stated floor (B11); the guardrails env-file allowlist (O10); verify's tag policy only if a bad tag blocks someone (O19).
- **Blocked:** M5 on a participant; Codex lifecycle hooks on an isolated login; the hosted delivery adapter on approval; a model session in a clean configuration on a login; savings claims on O2.
- **Watch out:** eval runs differ from live sessions: `skillgate` is not on the shell path and a sandbox denies some file checks, so read the first transcript before any score. The first live Codex rehearsal left trusted-project entries for two deleted temporary folders in `~/.codex/config.toml`; they were removed on 2026-09-16. The terminal `claude` is 2.1.92; pass the editor-bundled 2.1.273 explicitly to evals and rehearsals. Lane branches and worktrees are merged; their open items are now in docs/COVERAGE.md, and removing a worktree deletes its untracked `LANE_REPORT.md`. `codex/*` branches are superseded history. New decisions and lessons are entry files (`skillgate record`), not appended to the monoliths. Paid checks: evals, the live probes, `live-clients.mjs`, `company-release.mjs --with-eval`.

## Earlier

### 2026-09-16 18:12 EDT
- **State:** Milestones are unchanged since the integration: M1 verified locally; M2 verified on Claude Code (live sessions and the offline rehearsal); M3 verified at install level on Claude Code and Codex (18 of 18); M4 verified locally; M5 not started because it needs a person. Since then: the follow-ups that needed no owner input are closed (O4, O5, O13, O18, O20), published as 366468a with CI run 35153435426 green and every step read. Eval cases for the task and security skills are added and run (B10). Their first run found a skill fallback that contradicted the project instructions, unrequested work after a set-up request, a PATH lookup that failed on an unreadable entry, and a judge wording error; all are fixed (e87d2d1), and with the sonnet judge the task case scores 1.00 (difference 0.39) and the security case 1.00 (difference 0.33), evidence/e87d2d1.../. O12 is closed: the recorded eval command uses a sonnet judge. Versions: workflow 0.3.3, guardrails 0.2.0, context-hygiene 0.1.3. Published: pushed to `main` with this note; the CI result of this push is read after it and belongs in the next note. Installed: nothing from this marketplace in the owner's own Claude Code or Codex configuration; installs exist only in throwaway homes used by rehearsals and probes. Released: none. Verified: the rehearsal installs only.
- **Next:**
  1. Owner inputs, smallest first (docs/BACKLOG.md): `CODEX_HOME=<folder> codex login` once, then `node scripts/rehearsals/live-clients.mjs --claude <2.1.273 path> --codex <path> --codex-home <folder>` (B2); one login inside a fresh `CLAUDE_CONFIG_DIR` (B3); approval to create a private throwaway GitHub repository for the delivery adapter (B4); applicability decisions for this repository's 15 security controls (B7); a version and the owner's signing key for the first release (B6); a person for the M5 rehearsal (B1, docs/rehearsals/NEW_BUILDER.md).
  2. Without owner input: the guardrails env-file allowlist (O10); verify's tag policy only if a bad tag blocks someone (O19).
- **Blocked:** M5 on a participant; Codex lifecycle hooks on an isolated login; the hosted delivery adapter on approval; a model session in a clean configuration on a login; savings claims on O2.
- **Watch out:** eval runs differ from live sessions: `skillgate` is not on the shell path and a sandbox denies some file checks, so read the first transcript before any score. The first live Codex rehearsal left trusted-project entries for two deleted temporary folders in `~/.codex/config.toml`; they were removed on 2026-09-16. The terminal `claude` is 2.1.92; pass the editor-bundled 2.1.273 explicitly to evals and rehearsals. Lane branches and worktrees are merged and can be removed; `codex/*` branches are superseded history. Paid checks: evals, the live probes, `live-clients.mjs`, `company-release.mjs --with-eval`.
