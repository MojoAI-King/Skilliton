# Handoff

Kind: Living.

## RESUME HERE

Written: 2026-09-23 13:44 EDT

- **State:** Release 1.2.0 is signed (tag skilliton-release/1.2.0 on 63b0119, manifest sha256 2de2193b9297), pushed, approved in a fresh clone (4 approved) and VERIFIED 8 of 8 here (user scope on 1.2.0; project scope pinned at 1.1.0). It carries every fix from the three cold reviews of 029f159: guardrails 0.9.0 (option prefixes, --no-force, -c and GIT_CONFIG push forms, unresolved variables, the commit-path cap, discard verbs, record-deletion forms, settings writes, .skilliton-off, dry-run honoured), workflow 0.23.0 (gate log and skill writers refuse links), the template default, SECURITY.md, THREAT_MODEL, README, INSTALL. The history was rewritten the same day to remove 311 AI co-author trailers (owner ran it; trees unchanged; tags re-signed; GitHub lists one contributor). Not done: the owner's read of the local regrade (running) and the Codex regrade; nothing is acted on until both are in. Evidence: checks.mjs 78 pass 0 fail 2 skipped of 80 on both the merged tree (df19d80, before the rewrite) and the release commit 63b0119; CI green on 63b0119 (run 35893765684); whole-history audit 0 findings in 742 files; red team 65 of 65 caught, 0 regressions, slowest 284 ms; fresh clone release list 4 approved 0 unapproved; skilliton verify all 8 VERIFIED; contributors API and stats list MojoAI-King alone.
- **Next:** Read the regrade scorecard and the owner's Codex findings together, then decide with the owner what to fix; restart the client for the 1.2.0 hooks; pin the project scope with skilliton pin --latest; delete the local branches tmp/guard-merge and lane/guard-review-0923 by hand
- **Blocked:** Only the owner can do these: read the Usage screen for the token window (turns the meter's reconstruction into a number that may be stated), report the end-of-day maintain minutes before and after, the extension keyboard checks and the other rows of docs/OWNER_TESTS.md, a clean macOS account, a Codex login, a Windows machine with a Claude login, an endpoint security product to test under, and a participant for the new builder rehearsal
- **Watch out:** Restart the client before the new hooks run (workflow 0.22.0, guardrails 0.8.0, context-hygiene 0.3.1 installed at 10:00 EDT). The lane folder ~/Desktop/Skilliton-lanes/instruction-size still has its branch checked out because the guard refused moving its stale receipt; remove that file by hand, detach, delete the branch. Windows stays not supported (B80, no Claude Code session there). Read the clock before typing a time
- **Git:** main @ 63b0119, 3 uncommitted

## Earlier
