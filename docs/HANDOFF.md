# Handoff

Kind: Living.

## RESUME HERE

Written: 2026-09-16 21:08 EDT

- **State:** Skilliton is the product name (formerly Skillgate); technical names such as the `skillgate` command, marketplace and `.skillgate/` paths are kept for compatibility (docs/BRANDING.md). Workflow 0.5.1, guardrails 0.2.0, context-hygiene 0.1.3. Milestones: M1 and M4 verified locally, M2 on Claude Code, M3 at install level on Claude Code and Codex, M6 (make it yours) and M7 (one command per machine, `skillgate join` and `join --undo`) verified locally, M5 and M8 not started. Published and read: f8ae8af (M7) passed CI run 35166538574 and the branding commits 214eb17 and 1f8a9da passed CI run 35169216613, each 31 of 31 steps, including the join tests (36 of 36) and the guides check on Linux; the public-safety scrub is INCOMPLETE there by design (no private name list in CI). The owner's positioning discussion is now docs/POSITIONING.md: how a company runs Skilliton, company, project and personal layers ("guide on the laptop, enforce at the merge"), which value claims have evidence (team cohesion and continuity measured; skill quality measured for named eval cases only; code cleanup not built; time and cost not measured), a skeptical lead's questions, and candidate next steps, recorded as backlog rows B14 to B18. Installed: nothing from this marketplace in the owner's own Claude Code or Codex configuration. Released: none. Verified: rehearsal installs only.
- **Next:**
  1. The owner plans to demonstrate the build in about a week (user-stated on 2026-09-16, so around 2026-09-23). The owner chooses the order of B14 (two-developer live demonstration), B15 (team view for a lead), B16 (measured comparison against plain Claude Code), B17 (code-quality pack), B18 (installs pinned to release tags) and M8 (B13). The recommendation given was B14 first, then B16, since the demonstration shows cohesion and the comparison decides what time or cost statement can be made honestly.
  2. Without owner input: B11 (Node.js 18) and O10.
  3. Owner inputs, smallest first: `CODEX_HOME=<folder> codex login` (B2); one login inside a fresh `CLAUDE_CONFIG_DIR` (B3); approval for a throwaway GitHub repository (B4); security applicability decisions (B7); a version and signing key for the first release (B6); a person for M5 (B1).
- **Blocked:** M5 on a participant; Codex lifecycle hooks on an isolated login; the hosted delivery adapter on approval; M8's live session on a login; a GitHub install ending VERIFIED on the first signed release; any time or cost statement on B16 and O2.
- **Watch out:** another session committed the branding change on `main` while this session worked; check `git log` against the last handoff before editing shared records. Use Skilliton in new text and keep the compatibility names. A client binary must not run during a check (every Codex invocation writes into its home), and join's receipt is untrusted input. The machine rehearsal reads github.com (`--no-github` offline). Pass the editor-bundled Claude Code 2.1.273 and Codex binaries explicitly. Evidence folders are dated in UTC. New decisions and lessons are entry files. Paid checks: evals, the live probes, `live-clients.mjs`, `company-release.mjs --with-eval`.

## Earlier

### 2026-09-16 21:03 EDT
- **State:** Branding cleanup is done locally on `main`: Skilliton is the evolved product name, formerly Skillgate. Current docs, diagrams, instructions, plugin descriptions and runtime messages use Skilliton. Workflow is 0.5.1. Existing command, marketplace, configuration and stored compatibility strings retain `skillgate`; docs/BRANDING.md explains them. All offline checks passed, with the unchanged project rehearsal passing 9/9 from a committed candidate snapshot after the first run mixed the new runtime with the previous committed harness. Strict marketplace and all three plugin validations passed. Branding code commit `214eb17` is local, not yet pushed at the time of this note; the only remaining uncommitted file is this handoff. Verify publication and CI from GitHub rather than inferring them from the note. No signed release or installation was performed.
- **Next:** Verify the branding commit and GitHub description are published and read CI. Then continue M8 from docs/BACKLOG.md B13, first recording its contract; B11 and O10 remain open. M5 still needs a real participant.
- **Blocked:** Existing milestone gates are unchanged: isolated client logins, hosted delivery rehearsal approval, applicability decisions, a signing key and version for the first release, and a participant. Security evidence is 0/15 current; all 15 controls remain missing and undecided.
- **Watch out:** The earlier separate repository/product naming decision is superseded. Historical evidence retains its recorded names and versions. Existing prepared projects receive the branded harness through the normal instruction migration or explicit `skillgate harness --apply`. The project rehearsal clones committed HEAD, so an uncommitted harness change causes its adoption step to compare different versions; validate a complete candidate snapshot before committing.
