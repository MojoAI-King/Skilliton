# Handoff

Kind: Living.

## RESUME HERE

Written: 2026-09-16 21:03 EDT

- **State:** Branding cleanup is done locally on `main`: Skilliton is the evolved product name, formerly Skillgate. Current docs, diagrams, instructions, plugin descriptions and runtime messages use Skilliton. Workflow is 0.5.1. Existing command, marketplace, configuration and stored compatibility strings retain `skillgate`; docs/BRANDING.md explains them. All offline checks passed, with the unchanged project rehearsal passing 9/9 from a committed candidate snapshot after the first run mixed the new runtime with the previous committed harness. Strict marketplace and all three plugin validations passed. Branding code commit `214eb17` is local, not yet pushed at the time of this note; the only remaining uncommitted file is this handoff. Verify publication and CI from GitHub rather than inferring them from the note. No signed release or installation was performed.
- **Next:** Verify the branding commit and GitHub description are published and read CI. Then continue M8 from docs/BACKLOG.md B13, first recording its contract; B11 and O10 remain open. M5 still needs a real participant.
- **Blocked:** Existing milestone gates are unchanged: isolated client logins, hosted delivery rehearsal approval, applicability decisions, a signing key and version for the first release, and a participant. Security evidence is 0/15 current; all 15 controls remain missing and undecided.
- **Watch out:** The earlier separate repository/product naming decision is superseded. Historical evidence retains its recorded names and versions. Existing prepared projects receive the branded harness through the normal instruction migration or explicit `skillgate harness --apply`. The project rehearsal clones committed HEAD, so an uncommitted harness change causes its adoption step to compare different versions; validate a complete candidate snapshot before committing.

## Earlier

### 2026-09-16 20:25 EDT
- **State:** M7 (one command per machine) is built and verified locally. `skillgate join`, run from a full clone of the company skills repository, adds the company marketplace to Claude Code and Codex, installs the plugins the team template enables, trusts the release signers from an out-of-band file, writes a `skillgate` launcher into `~/.local/bin` (never editing a shell profile), and ends with verify; a receipt per company lets `join --undo` remove exactly what join added. verify from an installed copy now takes its source from the receipt, and verify for Codex falls back to the Claude catalog without a signed release. Installing from a GitHub source was measured for the first time on both clients (docs/CLIENTS.md), and so were two Codex behaviors the stand-in lacked: a missing `CODEX_HOME` is refused, and every Codex run writes into its home (lesson entry `2026-09-16-the-real-codex-refused-a-missing-home-an-2784`). An independent security-first review then reproduced twelve problems in join (the worst: a tampered receipt could make undo delete an unrelated file); all are fixed in 5358f37 with a regression test each (lesson entry `2026-09-16-undo-trusted-its-own-receipt-so-a-tamper-15cd`). Evidence: `scripts/join.test.mjs` 36 of 36; machine rehearsal 8 of 8 on Claude Code 2.1.273 and Codex 0.154.0-alpha.6.2 after the fixes (evidence/rehearsals/2026-09-17-machine), including installs from this repository's GitHub source; every offline suite passes. Versions: workflow 0.5.0, guardrails 0.2.0, context-hygiene 0.1.3. Milestones: M1 and M4 verified locally, M2 on Claude Code, M3 at install level, M6 and M7 verified locally, M5 not started, M8 not started. Published: the M7 commits are pushed with this note; the CI result is read after the push. Installed: nothing from this marketplace in the owner's own Claude Code or Codex configuration. Released: none. Verified: rehearsal installs only.
- **Next:**
  1. Without owner input: M8, autopilot on arrival (docs/BACKLOG.md B13): record the design in docs/CONTRACTS.md first (how an unprepared repository is offered preparation at session start, and how a delivery policy draft is built from detected test commands), then build and rehearse it on three kinds of repository. Also B11 (Node.js 18) and O10.
  2. Owner inputs, smallest first: `CODEX_HOME=<folder> codex login` (B2); one login inside a fresh `CLAUDE_CONFIG_DIR` (B3, also M8's live session); approval for a throwaway GitHub repository (B4); security applicability decisions (B7); a version and signing key for the first release (B6), which would also let M7's GitHub path end VERIFIED; a person for M5 (B1).
- **Blocked:** M5 on a participant; Codex lifecycle hooks on an isolated login; the hosted delivery adapter on approval; M8's live session on a login; a GitHub install ending VERIFIED on the first signed release; savings claims on O2.
- **Watch out:** a client binary must not run during a check: every Codex invocation writes into its home. join's receipt is untrusted input; keep undo bound to the trust path and the exact launcher text. The machine rehearsal reads github.com (pass `--no-github` offline). The terminal `claude` is 2.1.92 and Codex is not on the shell path; pass the editor-bundled Claude Code 2.1.273 and Codex binaries explicitly. Evidence folders are dated in UTC. New decisions and lessons are entry files. Paid checks: evals, the live probes, `live-clients.mjs`, `company-release.mjs --with-eval`.

### 2026-09-16 19:45 EDT
- **State:** Since the last note, the owner set the roadmap after M5 (decision entry `2026-09-16-roadmap-after-m5-make-it-yours-one-comma-b003`): M6 make it yours, M7 one command per machine, M8 autopilot on arrival, now rows in PLAN.md section 7. M6 is built and verified locally. `skillgate company init` gives a fork its own marketplace name, owner and team settings; `skillgate new-plugin` creates a company plugin, so the documented `new-skill --pack <company>` works on a fresh fork (it refused before: lesson entry `2026-09-16-a-documented-maintainer-command-refused-1178`); doctor's catalog check covers every pack. `scripts/rehearsals/fork.mjs` passed 7 of 7 on Claude Code 2.1.273 and Codex 0.154.0-alpha.6.2 (evidence/rehearsals/2026-09-16-fork): renamed fork, company plugin and skill, strict validation, signed release, installs verified on both clients, a project prepared with the fork's settings. docs/HOW-IT-WORKS.md walks the whole path with five Mermaid diagrams (all five rendered with mermaid-cli before commit) and is linked from the README; `scripts/docs.test.mjs` (in CI) checks the guides' commands, verbs, links and every doc's Kind label, and its self-test proves each check can fail. Versions: workflow 0.4.0, guardrails 0.2.0, context-hygiene 0.1.3. Offline suites all pass (CLI 181 checks). Milestones M1 to M5 are unchanged. Published: the code commit e2784bd and this documentation commit are pushed together; the CI result is read after the push. Installed: nothing from this marketplace in the owner's own Claude Code or Codex configuration (checked after the fork rehearsal). Released: none. Verified: rehearsal installs only.
- **Next:**
  1. Without owner input: M7 (docs/BACKLOG.md B12). Record the command's design in docs/CONTRACTS.md first, then measure `claude plugin marketplace add MojoAI-King/Skilliton` in a throwaway `CLAUDE_CONFIG_DIR` (public repository, no login), then build one previewable, undoable machine setup that ends VERIFIED on both clients. Also B11 (Node.js 18) and O10.
  2. Owner inputs, smallest first: `CODEX_HOME=<folder> codex login` (B2); one login inside a fresh `CLAUDE_CONFIG_DIR` (B3, also needed by M8's live session); approval for a throwaway GitHub repository (B4); security applicability decisions (B7); a version and signing key for the first release (B6); a person for M5 (B1), who can start from docs/HOW-IT-WORKS.md.
  3. After M7: M8 (B13).
- **Blocked:** M5 on a participant; Codex lifecycle hooks on an isolated login; the hosted delivery adapter on approval; M8's live session on a login; savings claims on O2.
- **Watch out:** `new-skill` and `import` write immediately, while `company init` and `new-plugin` preview until `--apply`; the guides say so. The fork rehearsal installs from a local folder, so installing from GitHub stays unmeasured until M7. Update, tamper, rollback and withdrawal are rehearsed only under the default marketplace name. `scripts/docs.test.mjs` proves a guide names real commands, not that they succeed from the reader's starting state; that is what the rehearsals are for. The terminal `claude` is 2.1.92 and Codex is not on the shell path; pass the editor-bundled Claude Code 2.1.273 and Codex binaries explicitly. New decisions and lessons are entry files. Paid checks: evals, the live probes, `live-clients.mjs`, `company-release.mjs --with-eval`.
