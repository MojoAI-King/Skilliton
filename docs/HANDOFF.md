# Handoff

Kind: Living.

## RESUME HERE

Written: 2026-09-19 01:36 EDT

- **State:** Wave 3 is published and green as 855d545 (CI run 35423599332, 53 of 53 steps, every step read): prepare drafts the dispatch fields and a delivery policy draft, delivery confirm moves it, task start --request, the Windows launcher with its test, the path measured in a live and a fresh session (workflow 0.10.0); area 02 at 11 of 15, 38 of 123 overall. This maintenance note (two lesson entries fd48 and ee5f, the status paragraph archived, PLAN M8 second increment, portable lessons 194 and 195 outside the repo, protocol unchanged at 2.73.0) is committed after the wave. Evidence: 49-step local suite PASS, scrub tree and history PASS, CI 35423599332 success; living-docs --check and report-card --check pass on the maintained tree.
- **Next:** 1. Wave 4 in plan mode from docs/REPORT_CARD.md: 03-01 VS Code column, 04-03 subagent hygiene, 07-01 dispatch command (03-02 Windows is the owner's); read each batch file first. 2. Next day's first session: compare the installed marketplace commit against f8a935c9b546 (01-01 item 5). 3. 04-01 item 5: measure two or three ordinary sessions at 600000 through the meter (B36). 4. Derive the task command's refusal loop from its parser table when task.mjs is next touched (lesson ee5f)
- **Blocked:** Owner pass at the end (docs/REPORT_CARD.md): interactive checklist, maintain minutes (01-02 item 5, 05-05), the live unprepared session (02-01 item 5), Windows run including skilliton.cmd, B7 decisions, signing key, hosted repository approval, M5 participant, Codex login. Any cost statement (PLAN.md sections 6 and 8)
- **Watch out:** Every checkpoint on main rewrites the indexes and, with --handoff, docs/HANDOFF.md; preview first without --apply. A Written or Updated time ahead of the clock is refused, so read date before typing one. prepare --apply in a repository with a test command now writes .skilliton/delivery.draft.json; no gate runs it until delivery confirm. A template edit makes migration 0100 pending here: run migrate --apply and commit the receipt
- **Git:** main @ 855d545, 5 uncommitted

## Earlier

### 2026-09-19 00:24 EDT
- **State:** Wave 2 is published and green as 33921a1 (CI run 35421068624, 52 of 52 steps, every step read): skilliton checkpoint now writes the task Handoff, the RESUME HERE block (--handoff, this note is its first live use) and the indexes (workflow 0.9.0); living-docs and backlog checks run in MAINTAIN.md step 2 and CI; 01-03, 05-01 and 05-02 at 100, 01-02 at 4 of 5. This maintenance note (lesson entry 43c7, status paragraph archived, protocol 2.73.0 outside the repo) is committed after the wave. Evidence: 48-step local suite PASS, scrub tree and history PASS, CI 35421068624 success; node scripts/living-docs.mjs --check and backlog.test.mjs pass on the maintained tree.
- **Next:** 1. Wave 3 in plan mode from docs/REPORT_CARD.md: 02-01 M8 second increment, 02-02 path everywhere, 02-04 stack detection; read each batch file first. 2. Next day's first session: compare the installed marketplace commit against f8a935c9b546 (01-01 item 5). 3. 04-01 item 5: measure two or three ordinary sessions at 600000 through the meter (B36). 4. Follow-up from decision 554d: the harness template line about checkpoints is edited in the next wave that changes the template anyway
- **Blocked:** Owner pass at the end (docs/REPORT_CARD.md): interactive checklist, maintain minutes before and after (01-02 item 5, 05-05), Windows run, B7 decisions, signing key, hosted repository approval, M5 participant, Codex login. Any cost statement (PLAN.md sections 6 and 8)
- **Watch out:** Every checkpoint on main now rewrites DECISIONS.md, docs/LESSONS.md and docs/STATUS.md indexes and, with --handoff, docs/HANDOFF.md; preview first without --apply. A Written or Updated time ahead of the clock is refused, so read date before typing one. The read guard is live here; the launcher under <home>/.local/bin is the skilliton on PATH
- **Git:** main @ 33921a1, 4 uncommitted

### 2026-09-18 22:58 EDT
- **State:** **Waves 0 and 1 are published and green** as **0f64946** (CI run 35417043380, **47 of 47 steps**, every step read); the wave 1 task record is closed as merged; this maintenance note follows it as its own commit. Not pushed: none. **01-01:** this machine is joined as company `mojoai` (receipt under the user config folder, launcher on the PATH, `verify` reads UNKNOWN VERSION for all three plugins because no signed release tags exist, B6; `evidence/live/2026-09-18-join-this-machine.md`); the three plugins are enabled at user scope on the editor-bundled Claude Code 2.1.276; this repository has `.claude/settings.json` from the template. The live rehearsal gained **L5** (read guard) and **L6** (compaction) and `--only`: L1 to L5 PASS in `evidence/rehearsals/2026-09-19-live-clients` (UTC date), L6 PASS in the `-run-4` folder after three failed attempts (kept: they recorded that the client ends a session that refills the window within 3 turns of a compaction three times over, and that a random-hash corpus cannot be compacted). Measured: the stream carries no PreCompact hook events on 2.1.276 (the journal is the witness); compaction fired at 76856 tokens against a 100000 window. B34 closed to the archive. The hook.mjs comment change bumps the workflow plugin to 0.8.1 (every plugin change bumps, CONTRACTS.md section 1). **04-01:** template window **600000, owner-chosen and unmeasured** (decision entry e7ee; the global 1000000 is owner-set; no per-subagent window exists in the agent frontmatter). **04-05:** `bashOutputMaxChars` 30000 and `taskOutputMaxChars` 32000 pinned (an earlier claim that no task cap key exists was wrong; the settings reference has it). Report card **14 of 123** (area 04: 10 of 19). Checks on this tree: the 43-step offline suite ran with 42 PASS and one FAIL (the report-card self-test's stale-table case: its mutation ticked one item and put the evidence on another once batch 01-01 had a real tick); the mutation was fixed (lesson entry 0a3c), the three report-card steps rerun green, the other 40 steps not rerun; scrub PASS (408 files); docs test 138 files; report card current.
- **Next:**
  1. Compact this session, then plan wave 2 in plan mode from docs/REPORT_CARD.md and the batch files it names.
  2. Owner: the 7-point interactive checklist in `docs/areas/01-autopilot-loop/batch-01-install-and-live-use.md` Notes, pasted into `evidence/live/<date>-owner-session.md` (B5 closes on it).
  3. Next day's first session: compare the installed marketplace commit against f8a935c9b546 to see whether the client pulled main on its own; record it in the join evidence file (01-01 item 5).
  4. 04-01 item 5: measure two or three ordinary sessions at 600000 through the meter, then revisit the number (B36).
  5. Wave 2: 01-02 rolling maintenance (B35), 01-03 and 05-02 bounded generated records, 05-01 backlog relationship test. Owner inputs unchanged: Windows run (03-02), demonstration rehearsal (around 2026-09-23), B29, B31, B4, B6, B7, B1.
- **Blocked:** as before (M5 participant, Codex hooks login, hosted delivery approval, first signed release, any cost statement, B3). Area 10 deferred by the owner.
- **Watch out:** `skilliton` now resolves on the login shell PATH (join wrote the launcher); `join --undo --company mojoai --apply` reverses the install. The context-hygiene read guard is live here: a whole Read of a non-image file over 50KB is refused, so read ranges. Rehearsal evidence folders carry the UTC date, which can be the next day. After ticking a batch item run `node scripts/report-card.mjs --apply`. Read the clock before writing a Written line.

### 2026-09-18 19:28 EDT
- **State:** The owner graded the repository against their own intent on 2026-09-18 (**overall C+**; strongest: make it yours B+, records and security B; weakest: any environment D, auto-harness D+, token efficiency C-). The card is saved as **docs/REPORT_CARD.md**, with one folder per area under **docs/areas/** holding 40 batch files, and **scripts/report-card.mjs** computing count-based bars (8 of 123 acceptance items ticked today, each with its evidence). One plan in dependency waves (decision entry of 2026-09-18). All of it is **built locally and uncommitted** on top of f8a935c: full suite 43 of 43 (the three new report-card steps included), scrub PASS, docs test 135 files labelled.
- **Next:**
  1. Owner: decide whether to commit and push wave 0 (`git status` lists the new and changed paths; the scrub and the suite passed on this tree).
  2. Wave 1, all on the owner's machine: **01-01** run join here and work one real session in this repository, filing B5 and B34 as evidence; **04-01** decide the compaction window from measurement (the global value on this machine reads 1000000; B36); **04-05** set the output caps explicitly in the team settings.
  3. Wave 2: 01-02 rolling maintenance (B35) with the owner's minutes measured before and after; 01-03 and 05-02 bounded generated records; 05-01 backlog relationship test.
  4. Owner inputs unchanged: the Windows run (03-02), the demonstration rehearsal (around 2026-09-23), B29's product, B31, B4, B6, B7, B1, B16's ceiling.
- **Blocked:** as before (M5 participant, Codex hooks login, hosted delivery approval, first signed release, any cost statement, a real login in a clean Claude Code configuration for B3). Area 10 (proof) is deferred by the owner.
- **Watch out:** after ticking an acceptance item under docs/areas/, run `node scripts/report-card.mjs --apply` or the CI check fails; a ticked item must end with `(evidence: ...)`. `skilliton` is still not on this shell's PATH (that gap is area 02 batch 02); run `node packs/base/plugins/workflow/runtime/skilliton.mjs`.

### 2026-09-18 18:22 EDT
- **State:** Token efficiency is part of the harness: **published and green** as **3d647cd** (CI run 35400451424, **44 of 44 steps**, every step read; the CI now runs the read guard and gate tests as their own steps). What shipped: the **read guard** as an enforced PreToolUse hook in **context-hygiene 0.3.0** (a whole-file read of a non-image file over 50KB is refused with a reason the model sees; 20 fixture tests); the **Session cost** section of the harness template, carried to prepared projects by migration 0003 (this repository migrated, 0100-instructions-ff8374b8e33a); **`skilliton gate`** in **workflow 0.8.0** (verdict from the exit status, full log under the repository's own Git folder, 17 tests); `autoCompactWindow: 200000` in the team settings template (a documented Claude Code setting, range 100000 to 1000000, verified against the settings reference); the **meter** `scripts/token-cost.mjs` reproducing the reference window with a fixture test and a NOT RUN reference check elsewhere, so **DECISIONS O2 is closed**; and the **M8 first increment**: an unprepared repository's session start prints a plain-words offer to prepare (lifecycle 32 of 32). An **independent security-first review** ran before publishing: one medium and six low findings, all code-level ones fixed with a test each (the gate opens its log before any check and treats an unwritable log as exit 3, a write error mid-run kills the process group, the tail carry is bounded, the read-guard log override is honoured only as an absolute non-symlink `.log` path, a numeric-string `limit` counts as a ranged read); the seventh (bare program names on Windows) is unverified and noted in docs/WINDOWS.md. The A2 rehearsal now migrates its clone before checking it (lesson entry 0316). Full offline suite on that tree **40 of 40**; scrub check and history scrub pass. The owner's two observations from use are backlog **B35** (rolling maintenance, because the end step costs 5 to 10 minutes) and **B36** (a 200000-token window is consumed fast under subagent-heavy work on the larger models); both are recorded, neither is designed. Installed on the owner's machine: nothing new. Released: none.
- **Next:**
  1. Owner: **B36 first**, because it decides whether people can use the harness at all: measure a subagent-heavy session with the meter, then choose the template default (the documented range is 100000 to 1000000) and say whether the read guard and the gate change the picture. B34 (the live read guard and compaction in a real session) is the same measurement.
  2. **B35**: design rolling maintenance (a checkpoint that also indexes and writes the handoff as it goes, so the end step is a short reconciliation), as a decision entry before code.
  3. Owner: run docs/WINDOWS.md on the Windows machine, now including the bare-program-name item, and send back what each command printed.
  4. Owner: rehearse the demonstration, `node scripts/demo-day.mjs --claude <path> --keep`, then walk docs/DEMO.md; the demonstration is around 2026-09-23.
  5. Then the Phase 3 order: M8's next increment (draft a delivery policy from detected test commands), M10 security audit, M11 routines, the Cursor spike, M12's macOS half. Owner inputs unchanged: B29's other half, B31, B4, B2, B3, B7, B6, B1, a spending ceiling for M14, a B24 milestone proposal.
- **Blocked:** as before (M5 participant, Codex hooks login, hosted delivery approval, first signed release, any cost statement, a real login in a clean Claude Code configuration for B3). Any statement about savings still comes only from the meter cross-checked against the Usage screen; none has been made.
- **Watch out:** `skilliton` is not on this shell's PATH; run `node packs/base/plugins/workflow/runtime/skilliton.mjs`. The allow-list test pins code lines by their text, so a refactor of a line it names fails the test until the pinned text is updated (that is the point). The rehearsal in scripts/rehearsals/projects.mjs clones the committed tree but runs the working-tree runtime, so a template change fails its adoption step until the clone is migrated. A docs subagent claimed the compaction window is a percentage; the settings reference says tokens. Read the clock before writing a Written line. Use the editor-bundled Claude Code 2.1.276 for `plugin validate --strict`; the terminal `claude` is 2.1.92.

### 2026-09-18 02:50 EDT
- **State:** Published and green: **ae51f5c** (CI run 35315939499, **42 of 42 steps**, every step read). 22 commits went out in that push (b0171a5 to ae51f5c), carrying **B26** a test that holds docs/IT-ALLOWLIST.md to the code, **B27** `skilliton preflight` with `join` refusing at exit 2 before it writes anything, **B28** the small-footprint test, **B29's first half** the preflight proved against blocks made on purpose, **B30** Windows through Git for Windows (built, **nothing run on Windows**), **B33** the fsmonitor hardening, and **B14** the demonstration kit. Then **ten adversarial review rounds**, each attacking the previous round's fixes: they found three security holes that would have been published (a secret scan narrowed to zero files by `GIT_INDEX_FILE`; a password-helper program a project could name through `GIT_ASKPASS`; a reachability check that reported an attacker's server as ok when the certificate variables were set), two fixes of mine that were themselves regressions, four test cases I believed written that had never been written, and about fifty smaller defects. Every finding is fixed, each with a case that fails when the fix is reverted. Decisions 1fba (a git call's environment is built, never inherited) and 14f7 (a hook can be silenced by the shell that starts it, and that is written down rather than defended) record the two architectural outcomes. The maintenance commit **345673c** follows it and is published and green too (CI run 35316790342, **42 of 42 steps**): plugin versions **workflow 0.7.1** and **guardrails 0.4.0** (both changed since their last bump, so installed copies update), three lesson entries, two decision entries, and these documents. Only this paragraph is newer than that run. Full offline suite **34 of 34**, guardrails **502 checks**, allow-list self-test **49 cases**, footprint self-test **34 cases**. Installed on the owner's machine: nothing. Released: none.
- **Next:**
  1. Owner: run docs/WINDOWS.md on the Windows machine and send back what each command printed, exactly as it appeared. Nothing about Windows is measured until then, and every document says so.
  2. Owner: rehearse the demonstration with the real client, `node scripts/demo-day.mjs --claude <path> --keep`, then walk docs/DEMO.md. The demonstration is around 2026-09-23.
  3. Then the Phase 3 order: M10 security audit, M11 routines, the Cursor spike (needs the owner's Cursor login on this Mac), and M12's macOS half on a separate Mac or virtual machine.
  4. Owner inputs: an endpoint-security product to test under (B29's other half), approval for a throwaway private repository (B31) and for the hosted delivery test (B4); unchanged: B2, B3, B7, B6, B1, a spending ceiling for M14, a B24 milestone proposal.
- **Blocked:** as before (M5 participant, Codex hooks login, hosted delivery approval, first signed release, any cost statement, a real login in a clean Claude Code configuration for B3).
- **Watch out:** the review loop is finished at ten rounds, and its lessons are the ones to carry: **revert a fix and see whether anything goes red** before believing it is done (three fixes shipped with no test that would notice their removal, and one rule's tests had never been written because the patch adding them stopped half way and reported nothing); **ask what the last round made worse**, because two rounds' fixes were regressions and the loop does not converge on its own. `BASH_ENV`, `SHELLOPTS=noexec` and an exported shell function can silence the guardrails hook entirely; the first and third are reported, the second cannot be, and all three are measured in `scripts/guardrails.test.sh` and written in docs/IT-ALLOWLIST.md. Only `GIT_CONFIG_NOSYSTEM` and `GIT_ATTR_NOSYSTEM` are treated differently on the network call, and they are left alone on local reads because every test here sets the first to isolate itself. Read the clock before writing a Written line. The enrollment rehearsal needs Docker and about ten minutes. Use the editor-bundled Claude Code 2.1.274 for `plugin validate --strict`; the terminal `claude` is 2.1.92.
