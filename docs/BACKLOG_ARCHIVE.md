# Completed backlog

Kind: Reference. Current work is in `docs/BACKLOG.md`.

Each closed item keeps its ID, outcome, closure date and evidence.

| ID | Outcome | Closed | Evidence |
|---|---|---|---|
| B42 | The harness line for dispatch reads enforced for Claude Code: the prompt hook directs `/workflow:dispatch` before any code and the stop hook asks once more when no lane plan was written; the event was seen reaching the installed plugin hook | 2026-09-22 | workflow 0.20.0 and 0.20.1; migration 0100-instructions-2c931b5edc63 here; `evidence/live/2026-09-22-dispatch-automation-live.md` |
| B62 | The managed block's guardrails line names all four blocked rules and the managed block guard; the template edit made migration 0100 pending here and it was applied in the same commit | 2026-09-22 | workflow 0.17.1; `templates/harness.md`; this repository's migration receipt under `.skilliton/migrations/` |
| B61 | Skilliton's files in a prepared repository cannot be removed by the assistant (guardrails denies `rm`, `rmdir`, `mv` and `git rm` aimed at `.skilliton/`, the records, the entry folders, `CLAUDE.md` and `AGENTS.md`, and a write that would take the managed block out of those two files is refused), a removal cannot land on the shared branch without an approver-signed commit, and a removal by a person is named at the next session start with the command that restores it, never restored silently | 2026-09-22 | guardrails 0.6.0 (`guard-bash.sh` rule 4, `hooks/managed-block-guard.mjs`), workflow 0.16.0 (`lib/delivery-persist.mjs`, `lib/records-restore.mjs`); scripts/guardrails.test.sh 669 checks, scripts/delivery.test.mjs 15 of 15, scripts/lifecycle.test.mjs 56 of 56; docs/CONTRACTS.md sections 2, 4, 11 and 14 |
| B55 | `dispatch` names a plan line shaped like an item whose ID is not `N<digits>` as a problem with its line and lane, and refuses; nothing is dropped without a word | 2026-09-22 | workflow 0.15.9; `runtime/lib/dispatch.mjs` NEAR_ITEM_RE; scripts/dispatch.test.mjs 23 of 23 |
| B38 | The first real dispatch: two lanes launched from their briefs, each lane's peak context under its ceiling by the meter's definition, both merged back to main the same evening | 2026-09-21 | `evidence/live/2026-09-21-dispatch.md`; batch 07-02 items 1 and 3; merges 22d98a7 and efb136a |
| B54 | `import` and `new-skill` preview by default and write only with `--apply`; their tests cover both paths and every guide example passes `--apply` | 2026-09-21 | lane/preview-0921 merged at efb136a (workflow 0.15.7); scripts/skilliton.test.mjs; README.md, docs/HOW-IT-WORKS.md, docs/OWNER_GUIDE.md, docs/CONTRACTS.md |
| B47 | The sweep of every `--apply` form with no target is a committed test: exit 2 and an unchanged tree for each form missing an argument, exit 0 and an unchanged tree for harness, index, migrate and prepare | 2026-09-21 | lane/sweep-0921 merged at 22d98a7; scripts/prepare.test.mjs (42 tests) |
| B4 (O15) | The GitHub delivery adapter ran on a hosted repository with branch protection: the workflow template unchanged, a ruleset requiring the check, a direct push refused, a defective pull request's check failing and the merge blocked. | 2026-09-21 | `evidence/live/2026-09-21-hosted-delivery-gate.md`; batch 09-01 items 2 and 3 |
| B31 (M12) | Install, a real update and verify ran from a private GitHub repository in a clean Claude Code configuration, over HTTPS with the machine's own credential helper, credential named by location only. | 2026-09-21 | `evidence/live/2026-09-21-private-repository.md`; batch 08-02 |
| B37 | One `OperationFailed` class: `runtime/lib/lifecycle.mjs` now imports and re-exports the one `runtime/lib/prepare.mjs` defines, so `guardCommand`'s catch covers both engines. | 2026-09-21 | `scripts/lifecycle.test.mjs`: "OperationFailed is one class, imported by lifecycle from prepare" asserts identity and instanceof across the two modules; lifecycle and prepare tests pass |
| B41 | One migration id pattern: `MIGRATION_ID_RE` is exported by `runtime/lib/migrations.mjs` and imported by `runtime/lib/release.mjs`, which had its own looser copy that accepted a doubled or trailing hyphen. | 2026-09-21 | grep finds one definition; `scripts/release.test.mjs` and `scripts/migrate.test.mjs` pass; release.mjs stays at its 614-line pin |
| B44 | The git wrapper check's message for unreadable options names only the remedy that branch has: write them as an object literal. The exception list it named is consulted by a different branch. | 2026-09-21 | `scripts/git-config.test.mjs:224`; the check and its `--self-test` pass |
| B46 | CI installs the client it validates the plugins with at a pinned version, `@anthropic-ai/claude-code@2.1.278`, the version tonight's runs measured, with a comment saying a bump is deliberate and recorded. | 2026-09-21 | `.github/workflows/checks.yml` step "Plugins validate"; the run for this commit |
| B51 | A string handed to a shell (`bash -c`, `sh -c`), `eval` or `xargs` that names git makes the guardrails hook ask for confirmation, with the reason that it cannot read inside such a string, instead of allowing with no output. Every direct spelling is still denied; a string without git is still allowed. Guardrails plugin 0.5.2. | 2026-09-21 | `scripts/guardrails.test.sh`: five new expectations in the section "a string handed to a shell or eval that names git asks", 549 checks pass; the skill text and the hook header say the new behaviour |
| B52 | `main` on the public repository is protected by a ruleset: no deletion, no force-push (non fast-forward), signed commits required. Required status checks were left off because the owner pushes straight to main and a required check cannot exist before the push. | 2026-09-21 | `gh api repos/MojoAI-King/Skilliton/rulesets` returns one ruleset, `protect-main`, enforcement active, rules deletion, non_fast_forward, required_signatures (id 23792740); every commit since the rename is SSH-signed (`git log --format=%G?` reads G) |
| B17 | A code-quality pack of four cleanup skills, each with an eval case run three times with the plugin and three times without it. One earned its place and ships (`split-a-file`, 1.00 against 0.89, a difference of 0.11); the dead-code, naming and test-strength cases scored the same in both arms, so those three skills are recorded in docs/not-shipped.md with their text kept beside the pack rather than deleted. The rule the batch asked for, that a skill ships only when its eval shows a difference, is what decided it | 2026-09-21 | commits 9e3c7f1, 1242dcb and 7d051a9; `evidence/1242dcb/`; docs/not-shipped.md; batch docs/areas/06-clean-code/batch-03-code-quality-pack.md |
| B45 | Every one of this repository's own audit alarms carries an allow marker with a reason. The whole tree audit reported 41 findings on 2026-09-21, none of them a real weakness: the guardrail's own refused patterns, the secret shapes its tests plant, and two manifest sentences. A gated repository would have rejected a push touching any of those files | 2026-09-21 | commit a9eea9e; 39 lines end with `skilliton-audit: allow <rule> <why>` across `hooks/guard-bash.sh`, `scripts/guardrails.test.sh`, `scripts/preflight.test.mjs`, `runtime/lib/trust.mjs` and `runtime/lib/preflight.mjs`, whose marker is folded onto an existing line because the file is pinned at 674 lines; the two JSON descriptions are reworded off the literal flag, which is the answer `runtime/lib/audit.mjs` gives for a line inside a string literal. `skilliton audit --range <root>..HEAD` over all 480 files: 0 findings, 45 allowed lines. docs/COVERAGE.md records both counts; lesson 2026-09-21-an-audit-finds-its-own-rules-written-dow-cc3f |
| B20 (M10) | `skilliton audit` reads the files a change touches and reports what is worth a person's attention, six rules over three controls, in all three places M10 asked for: the Stop hook says it once per working tree state, an installable `.git/hooks/pre-push` reports the range each push carries, and the shared repository's delivery gate **rejects** a push whose changed files carry a finding. Findings can be recorded as scoped security evidence whose sources are exactly the files that were read, so a clean observation goes stale the moment one of them changes. The eval half of this item (whether a skill helps a model act on the findings) moved to B17, which is where the eval run lives | 2026-09-20 | `runtime/lib/audit.mjs`, `runtime/lib/audit-run.mjs`, `runtime/lib/audit-install.mjs`, `runtime/commands/audit.mjs` (workflow 0.14.0); `scripts/audit.test.mjs` with `--self-test`, planted flaws asserted at their exact line; the gate proved to reject a planted flaw and accept the same commit cleaned, in `scripts/delivery.test.mjs`; `node scripts/lifecycle.test.mjs` for the Stop sentence, with a mutation check that drops it; docs/CONTRACTS.md sections 11, 12 and 14 |
| B34 | Live Claude Code sessions (2.1.276, headless) showed the read guard refusing a whole-file Read of a 60KB file with its reason, logging it once and letting a ranged read through, and a session compacting on its own at the `autoCompactWindow` a project's settings set, with the PreCompact and compact SessionStart hooks recorded and the Project state shown again; the compaction step took four runs, and the failed ones recorded that the client ends a session that refills the window within 3 turns of a compaction three times over | 2026-09-18 | `scripts/rehearsals/live-clients.mjs` steps L5 and L6; `evidence/rehearsals/2026-09-19-live-clients` (L5) and `2026-09-19-live-clients-run-4` (L6); docs/CLIENTS.md rows for the Read hook and the compaction window |
| B33 | Every git call the runtime makes disables a repository-configured fsmonitor program (`-c core.fsmonitor=false`), as the guardrails hook already did, so a copied repository's own `.git/config` cannot start a program at session start | 2026-09-17 | `NO_REPOSITORY_PROGRAMS` in `runtime/lib/journal.mjs`, used by journal, trust, collectors, delivery and prepare; `scripts/git-config.test.mjs` (6 cases, the first a positive control showing plain git does start it) |
| B27 (M12) | `skilliton preflight` checks a laptop against the allow list before anything is set up: every program found and started once from the parent that starts it in a session, every folder tested with a file it writes and removes, and the company repository asked for its branches; each item says what it stops (setup or sessions) and what IT would have to allow, and `join` runs it first and refuses before writing anything | 2026-09-17 | `runtime/lib/preflight.mjs`, `runtime/preflight/probe.sh`, `runtime/commands/preflight.mjs` (workflow 0.7.0); `scripts/preflight.test.mjs` proves it against real blocks (10 cases); docs/CONTRACTS.md section 13 |
| B26 (M12) | A test keeps docs/IT-ALLOWLIST.md matched to the code: every program the hooks, launchers and runtime start is named in section 1, every place the code reaches outside a repository is accounted for, a scenario in an empty home folder writes only inside the locations section 2 lists, and section 4's "no network of its own" holds | 2026-09-17 | `scripts/inventory.mjs` and `scripts/allowlist.test.mjs` (with `--self-test`, 16 cases); commit b0171a5; it found four programs and one path the page was missing |
| B28 (M12) | A test enforces the small footprint: no network code or downloader, nothing needing administrator rights, nothing that installs a service, agent, scheduled task or login item, no writes to system folders, every shipped file a text script with a shebang, no code built at run time, and only the file modes the product uses | 2026-09-17 | `scripts/footprint.test.mjs` (with `--self-test`, 19 cases); docs/IT-ALLOWLIST.md "The short version" |
| B25 | The project state report (`skilliton status` and the session start) reports a handoff Written time more than five minutes later than the machine's clock as needing attention instead of calling the handoff current; the maintain skill says to read the time from the clock (workflow 0.6.1) | 2026-09-17 | `handoffCheck` in `runtime/lib/lifecycle.mjs`; `scripts/lifecycle.test.mjs` reproduces the measured case (a time two hours ahead with a newer uncommitted change) and fails without the check; task 2026-09-17-b25-a-handoff-written-time-in-the-future-1de1; lesson 2026-09-17-handoff-times-typed-ahead-of-the-clock-h-95ba |
| B19 (M9) | Every current technical name is Skilliton; prepared projects move by migration `0003-skilliton-names`; earlier machine and repository state is named, never read as trust; guardrails and the delivery gate keep protection in force until a project moves; the `SG-` control IDs stay (O23) | 2026-09-17 | commits 5a67e74 and 1b626d1 (CI 35176795761, 34 of 34); evidence/rehearsals/2026-09-17-fork, -company-release, -projects, -machine-run-2 (8 of 8, including the GitHub install); `scripts/rename.test.mjs`; decision 2026-09-16-the-rename-moves-projects-by-migration-r-a319 |
| B12 (M7) | `skillgate join` sets up a machine in one command (company marketplace, plugins, release signers, a verify source, a terminal command) ending VERIFIED on Claude Code and Codex, and `join --undo` reverses it; installing from a GitHub source measured | 2026-09-16 | decision entry "Machine setup runs from a clone of the company repository, with a receipt"; evidence/rehearsals/2026-09-17-machine; `scripts/join.test.mjs` |
| O18 (from B8) | `verify` reports a file that lost the executable bit its release gives it as attention (exit 1), naming it | 2026-09-16 | DECISIONS.md "Follow-ups closed without owner input"; `scripts/release.test.mjs` |
| O20 (from B8) | One ID rule for tasks, decisions and lessons in `runtime/lib/ids.mjs` | 2026-09-16 | same decision; `scripts/records.test.mjs` |
| O5 | `setup.mjs --undo` keeps settings edits made after `--apply` and removes a file `--apply` created | 2026-09-16 | same decision; `scripts/setup.test.mjs` |
| O4 | The drift check reports DRIFT only when a declared model differs from what ran | 2026-09-16 | same decision; `scripts/drift-check.test.sh` |
| O13 | `scrub-check.sh --history` scans the branch being pushed; `--history-all` scans every ref (CI) | 2026-09-16 | same decision; `bash scripts/scrub-check.sh --self-test` |
| B10 | Eval cases for the `task` and `security` skills, with the skill, runtime and grader fixes their first run called for | 2026-09-16 | DECISIONS.md "Eval cases for the task and security skills, and what their first run changed"; evidence/06880a6... and evidence/e87d2d1... |
| O12 | The eval judge is sonnet; the default judge failed replies that met their rubric | 2026-09-16 | same decision; evidence/e87d2d1.../task-start-records-work-sonnet-judge |
