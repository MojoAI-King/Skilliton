# Changelog

Kind: Living. One entry per signed release, from its manifest under `releases/`, plus what is on `main` since. Plugin versions are what a machine sees; the release number is what it trusts.

## Unreleased

On `main` since 1.4.3. Plugins: workflow 0.26.0 (a new command group and a new skill, so a minor by the release skill's rule); the other three unchanged. Source: the owner's direction of 2026-09-25 to fold MojoComply's compliance work into Skilliton; decision 2026-09-25-compliance-lives-in-public-skilliton-as-39ef (assess, never certify; frameworks as data; a scope only with a named person; the sheet from evidence Skilliton already holds); task 2026-09-25-compliance-in-skilliton-frameworks-as-da-3e50, built in five lanes from f47142d and merged fast-forward. Nothing in it runs until someone runs `skilliton compliance`.

- **Frameworks as data (workflow 0.26.0, N1).** `scripts/frameworks-convert.mjs`, a dependency-free reader for the YAML constructs MojoComply's corpus uses and nothing else, converts its libraries into `packs/base/plugins/workflow/frameworks/`: seven libraries with 159 controls (hipaa-security-rule 58, hipaa-privacy-breach 24, health-wellness 21, ftc-safeguards 17, ny-dfs-500 16, part2-overlay 13, legal-safeguarding 10), the NIST CSF 2.0 spine, seven crosswalks to it, and the scoping knowledge base, each library with its sources and an attribution line. `soc2-tsc` and `pci-dss-v4` are never converted (AICPA and PCI SSC copyright). The source path is machine-specific and written nowhere: `FRAMEWORKS_SOURCE_DIR` names it, and `--check` (the committed JSON equals a fresh conversion) reports NOT RUN elsewhere. `scripts/frameworks.test.mjs` (93 checks, two of them only where the source is reachable) covers the reader's refusals, unique ids, a citation per control, every crosswalk id resolving into its library and the spine, and the baseline crosswalk on its own. The repository's own scrub check caught the first pass carrying a personal name and 331 dashes from the source's internal notes into the JSON; the converter now redacts the name to a role and normalizes the dashes, deterministically, so a fresh conversion and the committed files stay equal.
- **`skilliton compliance scope` (workflow 0.26.0, N2).** A port of MojoComply's scope scanner: the seven root description files and `package.json` one folder down, 21 detectors unchanged, the 512 KB cap, the negation window, three hits per signal, the same scoring and confidence. It proposes frameworks with the file and line of every signal, the intake questions no scan can answer, and the disclaimer; `--apply --decided-by <person>` records the recordable ones (a shipped library and a strong signal of current activity) in `.skilliton/compliance/scope.json`, additively, and is refused without the name. Nothing decides that a framework applies to an organization. `scripts/compliance-scope.test.mjs` plants each signal and clean look-alikes and checks the shipped knowledge base pairs every detector both ways.
- **`skilliton compliance sheet` and the baseline crosswalk (workflow 0.26.0, N3).** `frameworks/baseline-2-to-nist-csf-2.json` maps each of the 15 baseline controls to CSF 2.0 subcategories with a strength and a reason; the project owner approved it on 2026-09-25 and it is marked reviewed (the approval and the one arguable row, SG-ACCESS-CONTROL rated strong for PR.AA-05, are in decision 39ef). The sheet walks every in-scope control through its library's crosswalk to CSF and this one to the project's security records, read through `evaluateSecurity` and never re-implemented, and writes `docs/COMPLIANCE-CONTROLS.md` (or `compliance.sheetFile`) between its markers with a state per control (evidenced, partial, not_started, not_applicable), the records behind it and what a person must supply; the disclaimer and the independence line (`compliance.builtByCompany`) are always there. Pure over its inputs: a second `--apply` changes nothing; without `--apply` it says how many rows would change and exits 1 when the file is missing or stale. `scripts/compliance-sheet.test.mjs` runs a synthetic project through it.
- **The vocabulary check (workflow 0.26.0, N4).** `scripts/compliance-words.test.mjs` fails on "compliant", "certified", "certify" or "passes" in the sheet its test writes, the command's printed text, the compliance source, and every framework file outside a citation or requirement field, with a `--self-test` that proves it fails on four planted words. At merge it found five phrases in the converted remediation and note text; they are reworded in the converter (each with the count it must match, so a source change refuses the run) rather than exempted, so the rule holds over every shipped word.
- **`skilliton security status --json` (workflow 0.26.0, N9).** One JSON object, schema `skilliton.security-status/1`: catalog version, the evaluation's own time, one entry per control (id, title, applies, decidedBy, assessment, freshness, recordId, recordedAt, maxAgeDays) and the counts; the exit code is the report's. `evaluateSecurity` now returns the time it evaluated at, so the JSON never takes a second reading. `scripts/security.test.mjs`, 9 checks.
- **The compliance skill and its eval (workflow 0.26.0, N6).** `skills/compliance/SKILL.md`: propose, get a named confirmation, gather what Skilliton can, fill the sheet, list what a person must supply, and the vocabulary law. `evals/compliance-never-certifies/` sets up a patient-portal fixture and grades that the reply is honest, says no banned word, decides no scope alone, grounds itself in the sheet's states and fired the skill. `scripts/compliance-skill-words.test.mjs` fails on a banned word in the skill's text. Run twice, three runs per arm each: at a5d3b58 the score was 0.75 with and without the plugin (no reply attested anything, but every reply denied the claim in the framework's own word, which the regex check counts as a verdict too); the skill's section 4 was rewritten so the three words never appear at all, not in a negation or a restatement of the question, with what to say instead for each, and at 1e9de2e the score was 1.00 with the plugin against 0.67 without (evidence/a5d3b58.../ and evidence/1e9de2e.../compliance-never-certifies, the graders unchanged between the runs).
- **Prepare, the session start and maintain (workflow 0.26.0, N5).** `prepare` drafts `.skilliton/compliance/proposal.json` from the project's own files when neither it nor a confirmed scope exists, and writes `compliance.builtByCompany: false` once; the security README names the confirm command. A `compliance` check joins the lifecycle: `skilliton status` and the session-start block print one line after the security line (a proposal waiting for a named confirmation, with the count and the command; the frameworks in scope and whether the sheet is current, stale or missing; nothing proposed; or no proposal and no scope), comparing framework ids rather than the proposal's time, which a fresh clone re-dates. `maintain --apply` writes the sheet when a scope exists and says how many rows changed. The lane built against a guessed seam first and reconciled to the real modules at merge; running the full check list then found that a static import of the sheet module made a build without `evaluateSecurity` fail every check instead of one, so the sheet module is loaded at the point of use. `scripts/prepare-compliance.test.mjs` and `scripts/lifecycle-compliance.test.mjs` prove the drafting, the four lines and the maintain step on fixture projects with a planted signal; `prepare.test.mjs`, `lifecycle.test.mjs`, `rename.test.mjs` and `footprint.test.mjs` carry the changed counts. The harness template is unchanged: the block already says the session start shows the project state.
- **Measured before the port (N7).** MojoComply's own scanner on two real repositories, one health practice and one without health data, recorded under evidence/live/2026-09-25-compliance-two-repositories.md: strong HIPAA, health-guidance and PCI proposals with a moderate SOC 2 and state-privacy signal and a weak Part 2 watch on the first, nothing on the second; the owner's verdict column reads agreed for all eight, from the owner's one-sentence approval of 2026-09-25 of every waiting item. Re-running with the port is the batch's last item.
- **The licence check (decision 2026-09-25-the-soc-2-and-pci-dss-libraries-stay-out-8c57).** SOC 2 and PCI DSS stay in MojoComply. The PCI SSC terms forbid publishing or preparing derivative works of the Council's content without a separate written agreement; the AICPA's known notice grants copies for personal, intraorganizational or educational use only, not disseminated. A paraphrase library under MIT is a published derivative, so neither is converted until a rights holder grants it in writing; the scan keeps naming them as held back for their terms.
- **A merge-time fix on main.** `scripts/allowlist.test.mjs` failed after the compliance-skill merge because the eval fixture starts `bin/skilliton` three times through a variable; the `SHELL_DYNAMIC` table now names it.

## 1.4.3, 2026-09-24

Plugins: workflow 0.25.3, guardrails 0.12.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.4.3.json`; every entry below shipped on `main` between 1.4.2 and this tag. Numbered 1.4.3 at the owner's request; the release skill's own rule would call a new guard behavior a minor release. Source: the owner's rule of 2026-09-24 that nothing asks during work, built in lane/quiet-guard-0924 and merged fast-forward on 2026-09-24. Upgrading changes what the guard does on every machine that installs it: see the first entry.

- **Guardrails without prompts (guardrails 0.12.0).** The owner's rule of 2026-09-24: nothing asks during work. `guardrails.mode` in `.skilliton/config.json` (or `SKILLITON_GUARDRAILS_MODE` for one session; the stricter wins, an invalid value counts as strict and the session start says so) is `quiet` by default: a confirmation the assistant can rewrite (edits that would be lost when the tree is dirty, a path or branch held in a variable, a `git add` in a folder that is not a repository yet, a command too long or unbalanced to read) is refused with the fix; one the hook cannot read (a script outside the project, `eval`, a pipe into a shell, `env -S`) or a check that could not run lets the command run and writes one line to `<git dir>/skilliton/guardrails.jsonl` (time, mode, answer, reason; never the command text); only a rule being turned off (a settings file, a hook skip, a git alias, `BASH_ENV`) and saved work dropped for good (a stash, an unmerged branch, a worktree, the repository) still ask. `strict` asks for every one as before; `fleet` refuses every one. A command that would throw away edits runs when the tree has nothing to lose. Blocked commands are blocked in every mode. Measured before the change: 171 confirmations in 24 hours on the owner's machine, 168 approved; replayed through the new answers, 189 asks became 13 asks, 33 refusals with the fix and 143 noted runs. The five guardrails suites export `strict` so their cases keep meaning what they say; `scripts/guardrails-modes.test.sh` (61 checks) covers the modes, the two sources, the notes file and the session-start line, and closes stdin on its session-start calls after hanging for 21 minutes in a terminal. Decision entry 2026-09-24-guardrails-never-wait-on-a-person-except-649e; lessons 2026-09-24-a-count-of-permission-prompts-that-skips-a3a6 and 2026-09-24-a-test-proved-under-a-non-interactive-sh-db8c.
- **Workflow 0.25.3: the managed block says what the guard does in quiet mode.** The harness template's guard sentence ended with "unconfirmed discards", which quiet mode made untrue; it now says that what the guard can make readable is refused with the fix, what it cannot read runs and is noted, and it asks only before a rule is turned off or saved work is dropped, with "apply the fix" as the instruction. The template is 2,831 bytes, up from 2,690. Migration `0100-instructions-3ced7e976ae9` carries the sentence to a prepared project the next time `migrate --apply` runs; nothing changes on plugin update alone.
- **CI runs the modes suite.** `scripts/guardrails-modes.test.sh` is a step in `.github/workflows/checks.yml`, with stdin closed; it also passes 61 checks under a pseudo-terminal on the merged tree.

## 1.4.2, 2026-09-24

Plugins: workflow 0.25.2, guardrails 0.11.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.4.2.json`; every entry below shipped on `main` between 1.4.1 and this tag. The number is a patch by the release skill's rule: fixes to existing commands and one new meter option, no new command. Only workflow changed. Source: the backlog rows B89 (open since the batch of 2026-09-24), B91 and B92 (found while releasing 1.4.1 the same evening).

- **Workflow 0.25.2: `dispatch close` counts a lane that ran as an agent (B89).** Claude Code files a lane agent under the integrating window's session, so the lane folder has no transcripts of its own and close read 0 requests for all seven lanes of the 2026-09-24 batch. The meter's new `--lane-dir <folder>` reads the folder's own sessions and every subagent transcript whose `.meta.json` names a lane agent and whose first message names that folder and no other beside it, then the agents it started, linked by their `toolUseId`; each transcript is read once. Measured on the machine that ran the batch: three of its lanes read 197, 97 and 164 requests, with peak contexts of 493,049, 202,727 and 287,464 tokens against the 200,000 bound, all three past it, which the old reading hid. Close says how many lane agent transcripts it counted and how many it left out, and a meter that does not know `--lane-dir` is asked again the old way and says so.
- **Workflow 0.25.2: a check run keeps a Mac awake, and a failure across a sleep says so (B91).** The full run on the 1.4.1 release commit failed two steps whose tests took 1,039 s and 968 s, where each passes in seconds, while the Mac went into system sleep twice; started again under `caffeinate` it passed, and how a sleep turned into those exits is not known. `skilliton gate` and this repository's `scripts/checks.mjs` now start `caffeinate -i -m -s -w <their own pid>` on macOS, which ends with the run, and say on their first line whether anything holds the machine awake; a failing step or verdict reads `pmset -g log` and names any sleep inside that run. Nothing is held or read on Linux or Windows, and the line says so. Preflight checks for both programs on a Mac.
- **Workflow 0.25.2: a refused security record says which attached path and why (B92).** A record refused as SENSITIVE_PATH said only that some attached path looked credential-like or secret-shaped; a note whose name was 49 name characters in one run took eight previews to find. The refusal now names the entry (`artifacts entry 1`, `sources entry 2`) and the rule as a fixed phrase: a long encoded run in one path part, a credential word, a credential file or folder name, a key or certificate file, or the secret shape by name. The path itself is still never printed, since a path refused as secret-shaped may be a secret.

## 1.4.1, 2026-09-24

Plugins: workflow 0.25.1, guardrails 0.11.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.4.1.json`; every entry below shipped on `main` between 1.4.0 and this tag. The number is a patch by the release skill's rule: a security fix and corrections, no new command. Only workflow changed. Source: the security retest and the first eval run after 1.4.0, the same evening.

- **Workflow 0.25.1: dispatch never writes through a link.** `skilliton dispatch --apply` wrote a lane's brief into the file a committed symbolic link at `LANE_BRIEF.md` pointed at, outside the repository, and exited 0; the task record and the lane report were written the same unchecked way. A base commit holding a link at `LANE_BRIEF.md`, `LANE_REPORT.md` or a folder of a lane's task record path is now refused before any worktree is made, and each write is checked again in the new worktree. When a write fails after a worktree exists, the message names every worktree the run made; before, it said "No lane was created" while the failing lane's worktree and branch were still there.
- **Workflow 0.25.1: the last writers inside a repository check for links.** `company init`, `new-plugin`, `release create` and the delivery policy's confirm step now refuse a path through a link that leaves the repository, with nothing written. `scripts/write-sites.test.mjs` lists every file write in the shipped plugins with the reason it cannot land outside (B88), so a new one fails until someone says why it is safe.
- **Workflow 0.25.1: the security skill runs the audit when asked to audit code.** Its new first section sends a request to audit changed code, or to fix what an audit finds, to `skilliton audit`, the fix, and a second audit run until it prints nothing found, without opening evidence records. The first eval run of the `audit-finding-acted-on` case on 1.4.0 showed the skill sending the model to the evidence register instead, with about ten extra turns and the same fix (evidence/4bc1c2c/audit-finding-acted-on/SUMMARY.md).
- **Workflow 0.25.1: a held stop asks for the answer last.** When the stop hook holds a session for a checkpoint, the reminder asks the model to end with its answer to the user in a line or two, because the checkpoint had become the session's last message and the answer scrolled away above it.
- **Upgrade every copy, including a project-scope install.** Workflow 0.24.0 and earlier read a lesson or decision entry name such as `docs/lessons/2026-09-24-a-fix-that-hardens-the-writers-it-names-772e.md` as a secret-shaped path, which 1.4.0 fixed. A security record that cites one is therefore read as invalid by those versions, and an invalid record makes every control read invalid, by design. Measured on this repository: 0.22.0, 0.23.0 and 0.24.0 read 13 of 13 controls invalid from two such records, where 0.25.0 and 0.25.1 read 0 invalid. A laptop still on 1.3.0 or earlier sees that at every session start once someone on 1.4.0 or later records evidence citing an entry.
- **The eval case's graders measure what they say.** `audit-clean-after` counts only with the plugin, since without it there is no `skilliton` command to run; `fix-on-line` accepts comment lines above the fixed call, which two correct fixes had; its offline check reads the grader's own pattern instead of keeping a copy.

## 1.4.0, 2026-09-24

Plugins: workflow 0.25.0, guardrails 0.11.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.4.0.json`; every entry below shipped on `main` between 1.3.0 and this tag. The number is 1.4.0 by the release skill's rule: new commands, a new guard rule and new gate behavior. Source: the owner's word on 2026-09-23 to finish everything a session can, the second field report from a client repository (usage and savings), and an outside review's order of work (claims beside their limits first, then an independent newcomer and continuity trial, then security verification).

- **README claims, each beside its limit.** "What it saves" now says what each comparison task measured and what it did not: one model and one fixture per task; the plain setup had no instructions file, so the orientation result measures the hook against nothing; continuity is enforced at the reading end and only instructed at the writing end, where the two-session task finished 1 of 2 times in both setups. A short "The idea" section states the design rule (instructions are advice, hooks are guarantees). "Still to measure" names the end-to-end handoff first.
- **Pinning a machine to a release tag (B18), measured.** On Claude Code 2.1.278 a marketplace source takes `#<branch or tag>`, the clone lands exactly on the tag, an update keeps it there, and the plugins installed are that release's versions; a commit cannot be named. A decision records the tag pin as the download boundary and `skilliton verify` as the trust boundary (evidence/live/2026-09-23-marketplace-pinned-at-a-release-tag.md). Found while measuring: the client's marketplace remove, run inside a repository, also empties that repository's plugin settings; recorded as a lesson.
- **CLAUDE.md is read once per session (B75), documented.** A decision entry quotes the client's documentation: the file is read at session start and held in context on every turn, so a mid-session rewrite does not apply until the next session.
- **The second field report, filed.** evidence/live/2026-09-23-field-report-client-repository-2.md, sanitized: no client name, no dollar figure.
- **The comparison runner's third setup.** `scripts/rehearsals/comparison.mjs` gains an `instructions` setup (an ordinary CLAUDE.md asking for the same behaviors, no hooks) beside `without` and `with`, and task B's colleague commit now sits inside the local history, so the histories are related (B66). docs/COMPARISON_PROTOCOL.md records the third run's design.
- **Workflow 0.25.0: usage a client can check, from the second field report's six asks.** The meter ships inside the workflow plugin at `runtime/meter/token-cost.mjs`, so a prepared project runs it without this repository's scripts; `scripts/token-cost.mjs` stays as a shim. Batches end at maintain events, task closes and merges, not at merge commits alone, so a repository that only fast-forwards gets rows. The default scope is the project plus its lane folders, matched exactly. A committed ledger, `.skilliton/usage/ledger.jsonl`, keeps each batch's token counts after the client's transcripts expire. It is never read or written through a symbolic link at `.skilliton`, `.skilliton/usage` or the file, nor a file with a second hard link: a pre-release review found the first version appended a row to wherever a committed link pointed, outside the repository. `usage screen` records what the client's Usage screen reads, and `usage summary` says saved only when the meter and the screen agree. `dispatch close` marks each merged lane's task record merged (B82) and prints each lane's cost and peak context against `dispatch.contextCeiling`, now a known config key.
- **Workflow 0.25.0: every applicable security control has a record (B71, B78).** `security status --require-collected` fails when an applicable control has no record, and CI runs it over this repository. The findings live in their own file, `docs/SECURITY_FINDINGS.md`, and migration 0004 moves them there in a prepared project. Assessments were recorded for the eleven controls that had none and for the secrets control; `security findings` lists what is still open. `security record` accepts a decision, lesson or task entry file as a source or an artifact: an entry's file name read as an encoded secret, or its title word such as tokenizer as a credential, and was refused; a known token shape anywhere in a path is still refused.
- **Workflow 0.25.0: the stop hook and session start, from the hooks lane and the newcomer walkthrough.** The first stop of a session that changed files and recorded no checkpoint is held once, whatever its age (`checkpoints.holdFirstStop`, default true). The working tree fingerprint carries a content digest of the changed paths, so a second edit to a changed file re-arms the reminder. The reminder notices a task drifting from its criteria and offers a split, once per working tree state, never as a block. Session start names a skill copy under `.claude/skills/` that differs from the installed plugin's, says when the previous session ended with uncommitted changes and no checkpoint, and names a checkpoint written by hand instead of reading it as none.
- **Workflow 0.25.0: smaller fixes the walkthrough found.** A failing gate verdict names the tracked files its output mentions outside the change and the other node processes running at the start, each by its program and script file only, because a command line can carry a token; the delivery gate's rejection keeps a `node --test` run's failing test names. `task close --apply` refreshes the tasks index. `pin` on its own says the clone is already on the newest approved release. `verify` names a skills clone pinned to one release while the installed plugins match another. `doctor` outside a git repository says there is nothing to prepare. The maintain skill's first step is `skilliton maintain --apply`. Lane briefs bound each command to about eight minutes, because a lane agent silent for ten minutes inside one call is stopped as stalled (B87). The stop hook's dispatch reminder reads a numbered lane plan such as `LANES-8.md`, not `LANES.md` alone (B86).
- **Workflow 0.25.0: a machine pinned to a release stays on its tag (B18).** `skilliton pin` moves Claude Code's company marketplace to the release tag as well as the clone, and `skilliton join` with a release adds the marketplace at that tag. Every client call runs from a neutral temporary folder, because on 2.1.278 a marketplace remove run inside a repository emptied that repository's `.claude/settings.json`.
- **Guardrails 0.11.0: a sourced file is read, not refused unread (B84).** `source <file>`, `. <file>` and `sh`, `bash` or `zsh <file>` read the file's text through the same deny and ask rules as a command, one level deep, when it is a regular text file inside the project, reached through no symbolic link, at most 64 KB, and named once. Anything else still asks and says why. A Python environment's `bin/activate` goes through.
- **Workflow 0.25.0: no write through a link that leaves the repository.** `harness --apply` and `project-settings --apply` followed a committed symbolic link at `CLAUDE.md`, `AGENTS.md` or `.claude/settings.json` and wrote the file it pointed at, outside the repository (reproduced before this release; the same code is in 0.9.0 and 1.3.0); `dispatch merge` did the same for a record folder linked outside. Each component below the repository root is now read with lstat, and a link that resolves outside or to nothing, or a file with a second hard link, is refused with nothing written. A link inside the repository, such as `AGENTS.md` linked to `CLAUDE.md`, is followed as before. SECURITY.md lists it with the other limits.
- **Workflow 0.25.0: a smaller instruction block (B65).** The block every prepared project carries in CLAUDE.md and AGENTS.md is 2,690 bytes, down from 4,499, because every session reads it in full and pays for it on every turn; every enforced and instructed statement is kept. Migration `0100-instructions-db85286c7260` refreshes the block in a prepared project.
- **Workflow 0.25.0: the pre-1.0 migrations leave the plugin (B69).** Migrations 0002 and 0003, for projects prepared by the prototype or under the earlier product name, run from `node scripts/legacy-migrate.mjs` in a Skilliton checkout; `skilliton migrate` refuses such a project with that exact command. Migration 0004 is a content migration and is listed as pending in a layout-3 project whose backlog still holds the security findings section.
- **An eval case for acting on an audit finding (B48).** `audit-finding-acted-on` plants an interpolated `execSync` call, asks the model to run the security audit and fix what it finds, and grades that the flagged line is fixed with an argument list, the audit reads clean, nothing else was edited, and no allow marker silenced it. Its fixture and graders are proved offline by `self-check.sh`.
- **Kept on purpose:** the guard's tokenizer stays in awk. A Node parser matched it byte for byte on every case, but the mean decision time rose from 26.8 to 78.6 ms and Node would become a requirement; the decision is in `docs/decisions/`.

## 1.3.0, 2026-09-23

Plugins: workflow 0.24.0, guardrails 0.10.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.3.0.json`; every entry below shipped on `main` between 1.2.0 and this tag, the same afternoon. Source: a regrade of 63b0119 by three fresh reviewers (73 of 100, hire signal HIRE, security B) and a second cold review of the same commit; each fix ships with a test that fails against the code at 63b0119.

- **Workflow 0.24.0: the delivery gate protects the program that runs its checks.** A policy may list `protectedPaths`; without it, every file a check command names in the tree plus `.github/workflows/` is protected. A pushed commit that changes a protected path must be signed by an approver, checked before anything is extracted or run, so a rewritten check never executes; the combined result must hold the content an approved commit gave it. This repository protects `scripts/` and `.github/workflows/`. Reproduced before the fix: a push that rewrote the checks to exit 0 beside a failing test was accepted. `scripts/delivery-protect.test.mjs`; docs/DELIVERY.md; CONTRACTS section 14.
- **Workflow 0.24.0: smaller boundaries.** The gate's output reader bounds a line that never ends (400 characters, the same as `skilliton gate`); the skill writers refuse a hard-linked manifest; the session-start handoff hook prints one refusal line instead of following a symbolic link; evidence freshness hashes every listed file every time instead of trusting size and time (a same-size change with a restored timestamp now reads stale); `prepare --help` reads the layout number from the code.
- **Guardrails 0.10.0: the remaining gaps from the reviews.** A command text over 64 KB asks at once, unread (the old parser ran past the hook's 10 second budget on 1 MB, which the client reads as allow); `sed -i`, `perl -i`, `gawk -i inplace`, `ex`, `ed` and `sponge` aimed at a record, CLAUDE.md or AGENTS.md deny; a shell fed from a pipe, a decoder, a heredoc or a file that is not a committed, unchanged script under `scripts/` asks, and so do `source` and `.`; any write to, copy or move over, or removal of `.claude/settings.json` or `settings.local.json` asks, whatever the text says; a glob after `rm`, `rmdir`, `mv`, `git rm`, `find -delete`, `rsync --delete` or a `>` is matched segment by segment against every record path and denies on a match (`rm -rf docs/*`, `docs/t*`, `d*cs/tasks`), and `rm -rf *` at the root asks. The wall-clock cases moved out of the correctness suite into `scripts/guardrails-timing.test.sh`, which reports NOT RUN under load instead of failing. The hook header says Codex runs no hooks shipped in a plugin. `scripts/guardrails-review2.test.sh` carries every case with the decision at 63b0119 beside the decision now.
- **The local check runner fails the way CI fails.** `scripts/checks.mjs` runs each step under `bash --noprofile --norc -eo pipefail`, so a step whose first command fails reads FAIL locally as it does in CI; `scripts/checks-runner.test.mjs`. The eight tests bound to commits that the history rewrite renamed run again in a fresh clone (fixtures pinned by digest, the rest rebound), and a missing baseline fails instead of skipping unless the clone is shallow. The prototype's release commit is named by its rewritten hash.
- **Docs.** README says which comparison run's overhead was the instruction block and which was the range reads, and what the stop hook does after 20 minutes of unrecorded changes; INSTALL says whoever controls main controls the hooks on a trial machine; SECURITY.md lists the second review's limits with this release beside each; docs/IT-ALLOWLIST.md states the journal's retention; docs/THREAT_MODEL.md and the decision entry for the history rewrite carry the mapping from every cited hash to its rewritten one.
- **Known, disclosed:** `source <file>` and `. <file>` ask every time, including a Python environment's `activate`; reading the sourced file through the same rules instead is a backlog item.

## 1.2.0, 2026-09-23

Plugins: workflow 0.23.0, guardrails 0.9.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.2.0.json`; every entry below shipped on `main` between 1.1.0 and this tag. The number is 1.2.0, not 1.1.1, by the release skill's rule: new guard rules and a new gate refusal are behavior a machine notices. The history of `main` was rewritten once the same day, before this tag, to remove AI co-author trailers from 311 commit messages; the trees are unchanged and the earlier tags were re-signed (docs/decisions/2026-09-23-the-history-was-rewritten-once-on-2026-0-82b9.md).

Three independent cold reviews of `main` at 029f159 (2026-09-23) found the limits below. Each was reproduced before it was fixed, and each fix ships with a test that fails against the hook or the writer as it was at 029f159. docs/THREAT_MODEL.md, written the same day, says what the guard is and is not.

- **Guardrails 0.9.0: the guard reads git options the way git does.** A start of a dangerous long option (`--forc`, `--f`, `--mir`, `--no-ver`) is read as that option; `--no-force` takes back `--force` only, so `--force-with-lease --no-force` stays a forced push; `remote.<name>.push`, `push.default` and `alias.*` set through `-c`, `GIT_CONFIG_KEY_<n>`, `GIT_CONFIG_PARAMETERS` or `--config-env` are read as what they do; an unresolved variable in a force-push or an `rm -rf` asks; `--no-verify` on merge, rebase, am, cherry-pick, revert and pull is denied; `HUSKY=0`, `SKIP=` and `LEFTHOOK=0` ask; `rm`, `mv`, `chmod` and `truncate` aimed at `.git/hooks` are denied; a push with `--dry-run` (or a start of it) is honoured the way git honours it and allowed, after a red-team pass found the first cut denied it. `scripts/guardrails-review.test.sh` carries every case with the decision at 029f159 beside the decision now, and runs the push forms against a real bare remote to show what git itself does with them.
- **Guardrails 0.9.0: the commit path caps its content scan** at the add path's size (10239 KiB). A staged file over the cap is checked by name and the commit asks, naming the file; a 100 MB staged file took the old hook 23 seconds, past its 10 second timeout, and takes the new one under a second.
- **Guardrails 0.9.0: the discard verbs ask and the record-deletion forms deny.** `checkout -f`, `switch --discard-changes`, `switch -C`, `worktree remove --force`, `restore <path>`, `checkout -- <path>`, `rm -f <tracked>` and `rm -rf .git` ask; `find ... -delete` over a records folder, brace expansion, `$PWD` and `./x/../x` paths, `> <record>`, `truncate`, `cp /dev/null`, a heredoc onto `CLAUDE.md` or `AGENTS.md`, `rsync --delete`, a one-line interpreter call naming a record, and a differently cased spelling on a case-insensitive disk deny.
- **Guardrails 0.9.0: a Bash write that switches the hooks off asks.** A command whose output lands in `.claude/settings.json` or `.claude/settings.local.json` with `hooks`, `disableAllHooks` or `enabledPlugins` in the command, the file or a file copied onto it, and any command that creates `.skilliton-off`. A Write or Edit tool call onto those files is not covered yet; the skill says so.
- **Workflow 0.23.0: the writers stay inside the repository.** The gate's log is opened without following links and refuses a linked path or a hard link (exit 3, nothing written elsewhere); `new-skill` and `import` check every component of the destination, the plugin's `skills` folder and its `plugin.json` from the repository root down and refuse a link with nothing written (exit 2). `scripts/skill-writers.test.mjs` and a new case in `scripts/gate.test.mjs`.
- **The team settings template defaults automatic plugin updates to off.** A machine that holds client work moves by `skilliton pin --latest` and a signed release; the try-it path may turn it on by hand. Decision entry in `docs/decisions/`.
- **Docs.** SECURITY.md says what the guard is and is not and lists the limits above with the release that carries each fix; README leads with what was measured and says which install path tracks `main` unsigned; INSTALL names `ssh-keygen` and the tested Node version; docs/IT-ALLOWLIST.md names the one runtime git call that contacts a remote; docs/THREAT_MODEL.md is new.
- **Hygiene.** The allow-list writes scenario points npm's own cache and log folders inside its temporary home; the planted secret fixtures in `scripts/preflight.test.mjs` are assembled at run time so no line matches a scanner (nine allow-list entries removed); `scripts/prepare-interrupt.test.mjs` proves an interrupted `prepare --apply` is recoverable by the steps its lock names; the release test's cleanup retries on the same race the join test had.

## 1.1.0, 2026-09-23

Plugins: workflow 0.22.0, guardrails 0.8.0, context-hygiene 0.3.1, code-quality 0.2.1. Manifest `releases/1.1.0.json`; every entry below shipped on `main` between 1.0.0 and this tag. The number is 1.1.0 rather than the planned 1.0.1 because the release adds commands and gate behavior (decision 2026-09-23-the-night-s-release-is-1-1-0-not-1-0-1-b-46b5).

- **Security (workflow 0.22.0):** the shared-branch gate can no longer be switched off with a replace ref: every git call it makes sets `GIT_NO_REPLACE_OBJECTS=1`, and updates to `refs/replace/` are rejected. A push whose checks rewrite the gate's hook, approvers file or `skilliton.*` settings is rejected. Only SSH signatures count, with the other verifiers pinned off. `join` no longer prints a credential typed into a URL. Found by an adversarial review on 2026-09-22; each fix has a test that failed before it.
- **Guardrails (0.8.0):** closed the bypasses an adversarial review reproduced: force-pushes through pattern refspecs and `heads/<branch>`, deleting a protected branch on the remote, wrappers that take an argument (`env -u`, `nice -n`, `timeout`, `sudo -u`, `exec -a`, `caffeinate`), `git stage`, quote and heredoc shapes that hid a later command, a `GIT_DIR=` prefix, a secret written and staged in one command, and `git -c core.hooksPath=<dir>` skipping hooks like `--no-verify`. The assistant can no longer turn a rule off by writing `.skilliton/config.json`. The secret shapes now come from one list shared with the runtime, and the commit guard also catches a Google API key, including the public ones in a Firebase web config.
- **Install and session start (workflow 0.22.0, context-hygiene 0.3.1):** `.skilliton-off` quiets every workflow hook; `doctor` passes a correct marketplace install and reads `CLAUDE_CONFIG_DIR`; `company init` and `join` no longer print a credential typed into a URL; the context-hygiene checklist line is silent when its optional setting was never set up; the session-start handoff skips fenced examples as the Node parser does.
- **The stop hook (workflow 0.22.0):** a clean working tree is never held for a checkpoint; before this, committing the checkpoint's own records triggered a reminder on a clean tree.
- **Records in a busy checkout, from the same field report (workflow 0.22.0):** `prepare.recordHeader` sets the line records open with, for a host whose own docs check wants another form (the default is unchanged); the open-tasks index drops its Updated column, so a checkpoint no longer rewrites the status record when no task changed state; the session-start block says when another session looks active in the same checkout, instead of calling it interrupted.
- **Security evidence, from the same field report (workflow 0.22.0):** the secrets collector takes an allowlist with a required reason per entry, so a reviewed benign match stops being a permanent gap and stays visible as allowed; `security record` names the field it refused; `security status` says when the delivery policy the `tests` and `delivery-policy` collectors need is missing, and how to make it.
- **Maintenance is due sooner (workflow 0.22.0), from a field report:** a session committing straight to `main` makes no merge commit, and in a client repository 41 commits in one evening never made maintenance due. It is now also due after 15 commits since the last `skilliton maintain` (a checkpoint never clears that), and whenever the shared handoff is 15 or more commits behind HEAD (only committing a handoff clears that). The paragraph says plainly that a maintenance done without `skilliton maintain` is not in the journal. The managed block says so (migration 0100).
- **The release skill (workflow 0.22.0):** the checks run again on the manifest commit before `release sign` (B64).
- **The meter** prices `claude-opus-5-5` from the published pricing page (B63), and `scripts/token-direction.mjs` tables requests, context, compactions, read-guard refusals and gate runs per day.
- **INSTALL.md** for a person or an agent, a README front door, `docs/ARCHITECTURE.md`, and `scripts/checks.mjs` running inside a linked worktree.

- **From a pre-release review of everything since 1.0.0 (four lenses, every finding tried by a skeptic, 16 confirmed and 1 refused):** the guard reads `((git push -f); true)` and `$((git reset --hard) )` as the subshell and command substitution they are, not as arithmetic, which let a force-push, `--no-verify` and a work-losing reset through with nothing said; the settings file, `CLAUDE.md` and `AGENTS.md` are matched in any letter case, so `.skilliton/Config.json` on a case-insensitive disk is the settings file it is; `core.hooksPath` set through `GIT_CONFIG_KEY_<n>` is refused like `-c`, and `git config core.hooksPath` asks; a secrets allowlist entry is bound to the matched line's hash, never a line number; `.gitattributes` is `* -text`, so a CRLF file is committed as it is instead of leaving `release create` a change it cannot commit; the session-start handoff hook honors `.skilliton-off`; a record header shaped like a heading or a list item is refused; the handoff archive keeps its header line above archived entries. Each has a test that fails without the fix.
- **From the same field report, five more (workflow 0.22.0):** `maintain --apply` refreshes the secrets and delivery-policy security records when they are missing or stale, before it writes the findings block, and settles in one run: when the findings rewrite makes the record it just wrote stale, it collects once more (B81); `security applicability --propose` proposes which controls apply from a fixed list of signals in the tracked files (a dependency manifest, for example), with the paths behind each, writes `.skilliton/security/applicability-proposal.json` and decides nothing, and `--accept-proposal --decided-by <label> --apply` records it; a failing `skilliton gate` names the files that were already untracked or changed when it started and the machine's load, so a failure is read against the tree and the machine it ran on; `checkpoint` notes, without writing it into the record, a task that holds 15 or more checkpoints or whose first checkpoint is more than a day old; and on Windows every program the runtime starts by a bare name (`git`, a policy check's `node`) is looked up on PATH first, because Windows otherwise runs a same-named file in the current folder (B79). Each has its own test.
- **Attribution:** the licence, the marketplace owner and every plugin's author field read Mojo AI Services, LLC.
- **Code that reads top to bottom (workflow 0.22.0):** `scripts/lint-shape.test.mjs` holds every runtime line to 160 characters and every function to 80 lines, with the lines and functions already over pinned at their count so they can only shrink; the six longest functions (`migrate`, `join`, `verify`, `prepare`'s plan, `dispatch`'s plan and brief) are split into named steps, two into new files (`lib/prepare-plan.mjs`, `lib/dispatch-brief.mjs`), with no output changed by a word.
- **doctor (workflow 0.22.0), from a cold read:** a machine with node and without jq or python3 is no longer told to install them, because the hooks read JSON with whichever of the three they find first (the status line and the drift check, which call jq directly, are named in a note); in a repository that was never prepared, the next step is `prepare --apply`, which writes the instruction block and the records it names, instead of `harness --apply`, which wrote only the block.
- **Smaller instructions, from the field report (workflow 0.22.0, migration 0100):** the managed block in `CLAUDE.md` and `AGENTS.md` is cut from 8,297 to 4,499 bytes, keeping every rule and moving explanation to the skills and records it names; a prepared project gets the new block at its next session start or with `migrate --apply`. The session-start Project state block collapses layout, migrations, versions and records into one line when all four are ok, so a clean start costs one line instead of four. The sessions check now reads a session as live from any of its journal events, not only a session-start or a bare stop, which no hook writes, so "interrupted" is no longer said of a session that is open in another window.

## 1.0.0, 2026-09-22

The second signed release, and the first that runs its routines without a person: repositories are prepared and migrated at session start on a joined machine, maintenance and dispatch are asked for by the hooks, and Skilliton's own files cannot be removed by the assistant. Manifest `releases/1.0.0.json`, tag `skilliton-release/1.0.0`.

- workflow 0.21.0, guardrails 0.7.0, context-hygiene 0.3.0 (unchanged since 0.9.0), code-quality 0.2.1.
- Project layout 3.
- What changed since 0.9.0, newest first:

- guardrails 0.7.0: a command that passes only because the project turned a rule off gets a notice naming the setting (`systemMessage`, no permission decision), found by running that one rule again with only itself on (B50).
- workflow 0.21.0: `handoff.keepEarlier` (0 to 20, default 5) sets how many earlier notes stay in the handoff (B60); `--dir` accepts the repository root in another letter case on a case-insensitive filesystem (B56); `bin/skilliton` names Node.js 22 as the floor, the version CI runs (B11). The repository's own checks under `scripts/` are held to the 600 line ceiling with the files already past it pinned (B59), and the offline check list lives in `.github/workflows/checks.yml` alone (B43).
- workflow 0.20.1: the dispatch note and the stop hook's dispatch paragraph say small chores go through dispatch too (one lane), and the only reason to skip it is a prompt that is not a list of work; in the first live run the assistant did six small chores directly and then skipped dispatch as "not separate pieces of work".
- workflow 0.20.0: the prompt hook reads the prompt from `prompt`, the field Claude Code 2.1.278 sends (measured with a project hook that saved its own input); it read only `user_prompt`, the name in the hooks reference, so the dispatch note had never fired in a real session. The note now directs `/workflow:dispatch` before any code, and the stop hook asks once more in the same session when no `LANES.md` was written after that prompt (a `dispatch-reminded` event, once per prompt). The harness line reads enforced for Claude Code (B42).
- workflow 0.19.0: `skilliton maintain [--apply]`, the mechanical half of maintenance (indexes, security findings, a `maintain` journal event); the stop hook holds the session on an integration branch when a merge landed, or a day and a commit passed, since the last maintenance, once per commit, naming the command and the judgment half (`lib/maintain.mjs`); the harness block says so. On a joined machine a pending instruction migration is applied at session start the way preparation is, said in the block and left uncommitted.
- workflow 0.18.0: on a machine that has joined, a repository with no `.skilliton/config.json` is prepared at its first session start (`lib/auto-prepare.mjs`: the same files `prepare --apply` writes, left uncommitted, said in the block's first note); the join file and receipt carry `"prepare": "auto" | "offer"` (`company join-file --prepare offer`); an empty `.skilliton-off` at the root or a `skilliton-off` file inside `.git` keeps a repository out, and `SKILLITON_AUTO_PREPARE=off` turns it off for a session, each said in the block. The stop hook no longer asks for a checkpoint in a repository that is not prepared.
- workflow 0.17.1: the managed block's guardrails line names the removal rule and the managed block guard (B62); migration 0100 is pending in every prepared project until `skilliton migrate --apply`, which replaces the text between the markers only. The session-start records line in a never-prepared repository says once that none of the records is in Git instead of listing them twice.
- workflow 0.17.0: the `release` skill: a signed release cut in order (preconditions, manifest, signature, push, proof from a fresh clone), with an eval case in which an untracked file inside a plugin folder stops it before anything is written.
- workflow 0.16.0: the delivery gate rejects a push whose result removes what Skilliton keeps in the project unless an approver signed the commit that removes it (`lib/delivery-persist.mjs`); session start and `status` name a missing record with the command that restores it, and a tracked-but-missing `.skilliton/config.json` as removed by hand, in place of the offer to prepare (`lib/records-restore.mjs`). B61.
- guardrails 0.6.0: `rm`, `rmdir`, `mv` and `git rm` aimed at a `.skilliton` folder, the record files, the entry folders, `CLAUDE.md` or `AGENTS.md` are denied, with `skilliton remove --apply` as the route; a new `managed-block-guard.mjs` hook refuses a `Write`, `Edit` or `MultiEdit` that would take the managed block out of `CLAUDE.md` or `AGENTS.md`; `guardrails.protectRecords: false` turns both off. Without `CLAUDE_PROJECT_DIR` the project is the repository root of the command's folder. B61.
- workflow 0.15.9: `dispatch` names a plan line shaped like an item whose ID is not `N<digits>` and refuses instead of dropping it (B55).
- workflow 0.15.8: the security engine's file layer moved to `lib/security-io.mjs`; every public name re-exported, no caller changed.
- workflow 0.15.7: `import` and `new-skill` preview by default and write only with `--apply` (B54).
- workflow 0.15.6: the index says once, as a note, that a folder holds numbered entries from before this runtime, instead of one problem per file.
- workflow 0.15.5: `company join-file` writes a `skilliton.join/1` file outside any working tree; `join --from <file>` joins a machine with it.
- workflow 0.15.4: `join` names every missing flag at once and what the machine has already joined.
- workflow 0.15.3: the session-start handoff is labelled as a record, not an instruction.
- workflow 0.15.2: `join` judges the machine before the clone's releases, so the first release tag no longer refuses a join.
- workflow 0.15.1: the tree hash leaves a top-level `.in_use` entry out, so a client's in-use marker no longer reads as tampering.
- guardrails 0.5.2: a string handed to `bash -c`, `sh -c`, `eval` or `xargs` that names git asks first.
- code-quality 0.2.1: the three unshipped skills moved out of the installable plugin folder to `docs/archive/not-shipped-skills/`.
- Repository: README in a cold reader's order; `docs/README.md` as the index; `scripts/checks.mjs` runs CI's list locally; CONTRIBUTING, SECURITY and this file added; build-era documents archived. `docs/WHITE_PAPER.md`, `docs/PLAIN_GUIDE.md` and `docs/TROUBLESHOOTING.md` added, with PDFs built from them outside the repository.

## 0.9.0, 2026-09-21

The build-complete checkpoint: the first signed release. Manifest `releases/0.9.0.json`, tag `skilliton-release/0.9.0`, verified 7 of 7 from a fresh clone.

- workflow 0.15.0, guardrails 0.5.1, context-hygiene 0.3.0, code-quality 0.2.0.
- Project layout 3.
