{
  "name": "intake-service",
  "version": "0.3.0",
  "private": true,
  "description": "A small form intake service, used as the sample application in the Skilliton demonstration.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
