# Eval evidence for 60aed2cdab8e

Kind: Reference. Generated by `scripts/evidence.mjs` from a `claude plugin eval` run; do not edit by hand.

- Commit: `60aed2cdab8eb52c0b8adcbd9d737f9d0f36d556`
- Plugins: `workflow` 0.25.0
- Claude Code: 2.1.278; started 2026-09-24T21:59:40.603Z; 123 s; ablation with-without; partial: false
- Cost: about $2.27, the eval tool's list-price estimate for running this eval (not plan usage, not a savings figure)
- Raw result sha256: `0cf67d85f64e5622648141466c3b5c4ec1730bcef737bf5cf7d7bf2dcaae9e92`

Overall score with the plugin: 1.00; mean difference with and without: 0.00.

| Case | With plugin | Difference | Runs per arm |
|---|---|---|---|
| `audit-finding-acted-on` | 1.00 | 0.00 | 3 |

## Checks per case

Counts are runs passed out of runs. Indicator checks (whether the skill fired) are shown but not scored.

### `audit-finding-acted-on`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| audit-clean-after (indicator) | regex | 3 of 3 | not run |
| finding-read | tool_used | 3 of 3 | 3 of 3 |
| fix-is-real | llm | 3 of 3 | 3 of 3 |
| fix-on-line | regex | 3 of 3 | 3 of 3 |
| no-allow-marker | regex | 3 of 3 | 3 of 3 |
| no-other-edit | tool_used | 3 of 3 | 3 of 3 |
| no-other-write | tool_used | 3 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |

## Notes

## What changed since the first run, and what this run shows

Run 2026-09-24 at 17:59 EDT on the plugin committed as 60aed2c (the run reads version 0.25.0 because the bump to 0.25.1 came after it and changed nothing else in the plugin), the same case, command and ceiling as the first run on 1.4.0 (evidence/4bc1c2c/audit-finding-acted-on/SUMMARY.md). Two things changed between the runs, and both are named so the scores are not read as one effect:

- **The skill.** The security skill's new first section sends a request to audit changed code, or to fix what an audit finds, to `skilliton audit`, the fix, and a second audit run until it prints `nothing found`, without opening evidence records.
- **The graders.** `audit-clean-after` counts with the plugin only, because without it there is no `skilliton` command; `fix-on-line` accepts comment lines above the fixed call. Both were diagnosed from the first run's traces before they changed.

Read from the traces of this run: all three runs with the plugin ran `skilliton audit` before and after the fix and ended on `nothing found`; none opened a security record; each took 11 to 15 turns, against 22 to 24 in the first run. The three runs without the plugin took 10 to 14 turns and fixed the same line the same way. Every grader passed in all six runs.

## What this answers, and what it does not

- With the changed skill, the plugin sends the model through the audit, and the fix is verified by a second audit run rather than assumed, at about the same number of turns as the plain client. The fix itself was equally correct without the plugin, in this run and the first.
- The scores (1.00 with, 1.00 without) do not measure a gain in fix quality: this case's flaw is one the model fixes correctly on its own. What the plugin adds here is the verification step and the finding's rule and line, not a better fix.
- One case, three runs per setup, one model.

## Command

    claude plugin eval packs/base/plugins/workflow --case audit-finding-acted-on --trust-plugin --no-publish --scaffold -j 4 --output-dir <folder outside the repository> --keep-temp --judge-model sonnet --max-cost-usd 5 --allow-tools Bash Write Edit
