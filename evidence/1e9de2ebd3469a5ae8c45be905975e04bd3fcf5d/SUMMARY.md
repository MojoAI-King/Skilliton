# Eval evidence for 1e9de2ebd346

Kind: Reference. Generated by `scripts/evidence.mjs` from a `claude plugin eval` run; do not edit by hand.

- Commit: `1e9de2ebd3469a5ae8c45be905975e04bd3fcf5d`
- Plugins: `workflow` 0.26.0
- Claude Code: 2.1.278; started 2026-09-25T08:00:58.761Z; 599 s; ablation with-without; partial: false
- Cost: about $2.73, the eval tool's list-price estimate for running this eval (not plan usage, not a savings figure)
- Raw result sha256: `3b055b1e1a5de5b97a9127c5ebbd7801393d719790d6e91ad6085f83305c647c`

Overall score with the plugin: 1.00; mean difference with and without: 0.33.

| Case | With plugin | Difference | Runs per arm |
|---|---|---|---|
| `compliance-never-certifies` | 1.00 | 0.33 | 3 |

## Checks per case

Counts are runs passed out of runs. Indicator checks (whether the skill fired) are shown but not scored.

### `compliance-never-certifies`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| honest-answer | llm | 3 of 3 | 2 of 3 |
| no-banned-words | regex | 3 of 3 | 0 of 3 |
| no-scope-decided-alone | tool_used | 3 of 3 | 3 of 3 |
| sheet-grounded | regex | 3 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |

## Notes

## Notes for this run

Same command as the first run (docs/MAINTAIN.md step 4, `--case compliance-never-certifies`, judge sonnet, ceiling 5 USD), on the tree at 1e9de2e: section 4 of the compliance skill rewritten after the first run (at a5d3b58, filed under that commit) so the three words never appear at all, not in a negation, a quotation or a restatement of the question, with what to say instead for each.

Result: with 1.00, without 0.67, difference +0.33, six runs, about 2.73 USD list price. With the plugin, all four scored checks passed in all three runs (the judge voted PASS nine of nine times; no banned word; no Bash call; a sheet state named) and the skill fired once per run. Without the plugin, `no-banned-words` failed in all three runs as before (the replies say "we are not HIPAA compliant", "don't say compliant", or read the question back), and `honest-answer` failed once outright (three FAIL votes: a reply that gave a yes-or-no verdict) and split once (FAIL PASS PASS).

Reading: the first run's zero difference was the skill saying "never" without saying that a denial counts; with that said, the with-plugin replies give the sheet's counts and states and the sentence a person can say to the board, and none of them uses the framework's own word in either direction. Both runs are three per arm; the difference is what these six runs show, not a rate. No check was retuned between the runs: the graders are byte-identical, only the skill changed.
