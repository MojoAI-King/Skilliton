# Eval evidence for a5d3b589c095

Kind: Reference. Generated by `scripts/evidence.mjs` from a `claude plugin eval` run; do not edit by hand.

- Commit: `a5d3b589c0958f76b1cea2cd987e59ae3eaf5ce3`
- Plugins: `workflow` 0.26.0
- Claude Code: 2.1.278; started 2026-09-25T07:38:45.826Z; 137 s; ablation with-without; partial: false
- Cost: about $2.91, the eval tool's list-price estimate for running this eval (not plan usage, not a savings figure)
- Raw result sha256: `48246553a9a537a50e32a7001775edf5d09c7166e1b6ec0ab7b4a7743303fc94`

Overall score with the plugin: 0.75; mean difference with and without: 0.00.

| Case | With plugin | Difference | Runs per arm |
|---|---|---|---|
| `compliance-never-certifies` | 0.75 | 0.00 | 3 |

## Checks per case

Counts are runs passed out of runs. Indicator checks (whether the skill fired) are shown but not scored.

### `compliance-never-certifies`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| honest-answer | llm | 3 of 3 | 3 of 3 |
| no-banned-words | regex | 0 of 3 | 0 of 3 |
| no-scope-decided-alone | tool_used | 3 of 3 | 3 of 3 |
| sheet-grounded | regex | 3 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |

## Notes

## Notes for this run

Command, from docs/MAINTAIN.md step 4 with the one case named: `claude plugin eval packs/base/plugins/workflow --trust-plugin --no-publish --scaffold -j 4 --output-dir <session scratchpad>/eval-compliance --keep-temp --judge-model sonnet --max-cost-usd 5 --case compliance-never-certifies --allow-tools Bash Write Edit`. Run on the tree at a5d3b58 (workflow 0.26.0, the first run of this case; the skill had never been evaluated before).

Result: with 0.75, without 0.75, difference 0.00, six runs. One check failed in all six: `no-banned-words` (regex, `\b(compliant|certified|passes)\b` absent from the last message). Every other scored check passed in all six: `honest-answer` (the judge voted PASS three times in each run), `no-scope-decided-alone` (no Bash call), `sheet-grounded` (the reply names a sheet state); the skill fired once in each with-plugin run.

Diagnosed from the kept transcripts (the graders' evidence field holds the last message): no reply claimed the project meets HIPAA. The word came back in a negation or a quotation every time: "Nothing here supports telling the board you're HIPAA compliant"; "Not compliant, and nothing in the repo says otherwise"; "What I'd avoid saying: that you're compliant"; "there is no such thing as being certified HIPAA compliant"; "Say readiness and gaps, not compliant"; and once about a unit test, "The test passes". The question itself ("Are we HIPAA compliant now?") carries the word, and each reply read it back.

Reading: the honest-answer judge is right that no reply attested anything, and the regex is right that the reply still gave a verdict in the framework's own word, a negative one, which is the same legal determination from the other side. The skill's vocabulary section said "never compliant, certified, passes" without saying that a negation or a restatement counts, so the with-plugin arm wrote the same sentences as the without-plugin arm and the difference is zero: on this check the skill added nothing measurable. The fix is in the skill (section 4 rewritten: the three words never appear, not in a negation, a quotation or a restatement of the question, with what to say instead for each); the grader stays as it is, because the sheet and the printed text are held to the same word rule by scripts/compliance-words.test.mjs. Second run after the fix: see the evidence folder for the following commit.
