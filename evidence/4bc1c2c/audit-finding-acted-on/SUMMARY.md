# Eval evidence for 4bc1c2c13fe1

Kind: Reference. Generated by `scripts/evidence.mjs` from a `claude plugin eval` run; do not edit by hand.

- Commit: `4bc1c2c13fe14a6cccc1b5074476932713c3080b`
- Plugins: `workflow` 0.25.0
- Claude Code: 2.1.278; started 2026-09-24T21:22:41.360Z; 191 s; ablation with-without; partial: false
- Cost: about $3.60, the eval tool's list-price estimate for running this eval (not plan usage, not a savings figure)
- Raw result sha256: `c4dbef55d09fa99f50542589c54ada2dd6d1a8cce853d13daebced99d69e049b`

Overall score with the plugin: 0.62; mean difference with and without: -0.24.

| Case | With plugin | Difference | Runs per arm |
|---|---|---|---|
| `audit-finding-acted-on` | 0.62 | -0.24 | 3 |

## Checks per case

Counts are runs passed out of runs. Indicator checks (whether the skill fired) are shown but not scored.

### `audit-finding-acted-on`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| audit-clean-after | regex | 0 of 3 | 0 of 3 |
| finding-read | tool_used | 3 of 3 | 3 of 3 |
| fix-is-real | llm | 1 of 3 | 3 of 3 |
| fix-on-line | regex | 1 of 3 | 3 of 3 |
| no-allow-marker | regex | 3 of 3 | 3 of 3 |
| no-other-edit | tool_used | 3 of 3 | 3 of 3 |
| no-other-write | tool_used | 2 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |

## Notes

## Diagnosis of every failed check, from the kept traces

Run 2026-09-24 at 17:22 EDT on the workflow plugin as released in 1.4.0 (0.25.0), six runs: three with the plugin, three without it. Every failure below was read from the run's own trace.

- **audit-clean-after failed in all six runs, by construction.** It looks for `skilliton audit: nothing found` in the trace, and no run ever ran `skilliton audit`. With the plugin, the prompt "run the security audit and fix what it finds" loaded the workflow security skill, and that skill never names `skilliton audit`: it sent the model to the evidence register instead (`security status`, `security collect secrets`, `security record`, then `task start` and `checkpoint`). Without the plugin there is no `skilliton` command at all, so a grader marked for both setups cannot pass there. The grader measures a command the case never makes available to one setup and the skill never names to the other.
- **fix-on-line failed in two runs with the plugin although the fix was right.** All three runs with the plugin replaced the template-literal `execSync` with `execFileSync("tar", ["czf", archivePath, "--", dir])`, the fix the rule names. Two of them put a comment line above the call, and the grader's pattern requires the call on the first line of the function.
- **fix-is-real failed in two runs with the plugin, judged on the wrong message.** The judge reads the last message. In the runs with the plugin, the stop hook held the first stop (a session that changed files and recorded no checkpoint, new in 1.4.0), so the model's last message was about the task record and the checkpoint it then wrote ("Recorded. Task ... checkpoint ..."), and the answer about the fix was one message earlier. The run whose last message restated the fix passed.
- **no-other-write failed once with the plugin:** that run used the Write tool for a file other than src/backup.js while it did the register work.

## What this answers, and what it does not

- The fix itself was equally correct in all six runs, judged from the code each run wrote. On this case the plugin did not help the model act on the audit finding: its security skill did not route the request to the audit, and the plugin added about ten turns (22 to 24 with it, 12 to 14 without) of evidence records and the checkpoint the stop hook asked for.
- The scored difference (0.62 with, 0.86 without) is mostly the graders above, not the fix; it is not a measure of fix quality.
- One case, three runs per setup, one model. It says nothing about other findings or other skills.

## What changes because of it

The security skill names `skilliton audit` as the step for a request to audit and fix code, and says not to open evidence records unless asked; the case's graders are corrected (audit-clean-after with the plugin only, fix-on-line allowing comment lines, fix-is-real judged on the whole reply); then the case runs again on the changed skill.

## Command

    claude plugin eval packs/base/plugins/workflow --case audit-finding-acted-on --trust-plugin --no-publish --scaffold -j 4 --output-dir <folder outside the repository> --keep-temp --judge-model sonnet --max-cost-usd 5 --allow-tools Bash Write Edit
