# Release 1.5.0, 2026-09-25

Kind: Reference. What was run to cut, publish and prove release 1.5.0, with each verdict. No signer names and no key material; the signer is in the tag.

## Facts before anything was written

- The history rewrite came first (lesson 2026-09-25-a-history-rewrite-gives-every-rewritten-71cd): the owner re-created the 25 commits from the first lane record that held a home-folder path, re-signed, and pushed d5dbaa2 with ruleset 23792740 off for the push and back on (deletion, non_fast_forward, required_signatures). Checked on the rewritten clone before the push: no home path in main's history, the newest tree identical to the old main, the parent d9cad87 unchanged, the same author, committer, dates and message on all 25, 25 of 25 good signatures, only the two task records changed, 9 of 9 tags on the new main, `scrub-check.sh --history` exit 0. CI on d5dbaa2: success, 129 steps, 0 failed, 0 skipped, the first green main since the lane records were committed.
- The owner's approvals of the same day were recorded before it: the baseline crosswalk reviewed, the verdict column agreed, SOC 2 and PCI DSS held back (decision 2026-09-25-the-soc-2-and-pci-dss-libraries-stay-out-8c57).
- Changed since 1.4.3: workflow 0.25.3 to 0.26.0; guardrails 0.12.0, context-hygiene 0.3.1 and code-quality 0.2.1 have the same tree hashes as in 1.4.3 (77b2cc32a54a, 5c841c3da632, 8d52d6844940).

## A first release commit, taken back

- `release create --version 1.5.0 --evidence <23 files> --apply` wrote a manifest on 8103936, committed as 71c88fd with a checkpoint d2e07e9. The full offline check list on d2e07e9: 120 pass, 1 fail, 2 skipped of 123. The failing step was `scripts/gate.test.mjs`, two of 19 tests ("stopped by the timeout, not by the sleep ending"; "stopped by the policy's one second, not the gate's sixty").
- Rerun alone at a 1-minute load of 4.4: the same two failed, every test in the suite about 15 s, so not load. `pmset -g log` alone took 9 s (201,439 lines) and ran on every failing gate run (lesson 2026-09-25-reading-the-whole-power-log-made-every-f-12f0).
- Neither commit had been pushed or tagged. `git reset --soft 8103936` took them back without touching a file; the unsigned manifest was deleted and the release restarted.
- The fix, 8fc8dbc: `sysctl -n kern.sleeptime` first (about 20 ms), the power log only when the kernel's last sleep began after the failing step did. `scripts/gate.test.mjs` 19 of 19 in 14.8 s, from 132 s. CI on 8fc8dbc failed its footprint step (run 36178260049) because `/usr/sbin/sysctl` was not in the footprint check's system paths; that fix, c07882c, went green on CI (run 36178415326: 129 steps, 0 failed, 0 skipped).

## Manifest

- `release create --version 1.5.0 --evidence <24 files> --apply` on c07882c wrote `releases/1.5.0.json`, manifest-sha256 `1977bef3764a806e882d0f7391baebf3fefae006411f1cb09dfc3c588d78d774`. Evidence: the sixteen files 1.4.3 carried with this release's predecessor note (2026-09-24-release-1.4.3.md) in place of 1.4.2's, the quiet-mode decision, the compliance decision and the licence decision, the two-repositories note, the two eval summaries of the compliance skill, and lessons 71cd and 12f0.
- Committed as 529f247 with the changelog, README and INSTALL; the checkpoint committed as bcf994e before the full run, so the gated commit is the tagged one (the 1.4.3 slip, backlog B96).
- Full offline checks on bcf994e: **121 pass, 0 fail, 2 skipped, of 123**, exit 0 (the two skipped are CI-only), with the runner holding the Mac awake; the load reached 19 during the run and the gate suite passed.
- CI on bcf994e, the tagged commit: success, 129 steps, 0 failed, 0 skipped (run 36182109222).

## Sign, publish, prove

- `release sign 1.5.0 --apply`: the manifest committed and valid, no tag yet, every plugin folder matching; created `skilliton-release/1.5.0` on bcf994e, signed with SSH; `git tag -v` reads a good signature carrying manifest-sha256 1977bef3764a.
- `git push origin main` and `git push origin skilliton-release/1.5.0` at 15:50 EDT.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **10 approved, 0 unapproved, 0 withdrawn**, 1.5.0 with manifest 1977bef3764a on commit bcf994e1298e, and 1.4.3 still on 6ae2b2084a81.
- On this machine: `skilliton pin --release 1.5.0 --apply` from a neutral folder moved the marketplace to the 1.5.0 tag (removed and added again) and reinstalled the four plugins at user scope. It also left this checkout on a detached HEAD at the same commit, which it says; `git checkout main` put it back.
- The project-scope installs did not survive the marketplace move: `claude plugin update <p>@skilliton --scope project`, the step pin prints, failed four times with "is not installed at scope project". `claude plugin install <p>@skilliton --scope project` from the checkout installed all four; `.claude/settings.json` was byte-identical before and after (sha256 prefix 25c37949ecdd). Backlog B102.
- `skilliton verify`: **8 VERIFIED, 0 TAMPERED**: every company plugin at both scopes matches release 1.5.0 (workflow 0.26.0, 220 files; guardrails 0.12.0, 7; context-hygiene 0.3.1, 8; code-quality 0.2.1, 48). Restart the client to load them.
- The security register after the release: five records reassessed (SG-RECURRING-PATTERNS and SG-SECURITY-LOGGING, stale from the compliance batch; SG-SECURITY-TESTS, SG-COMMAND-INJECTION and SG-DEPENDENCY-RISK, stale from the fix and the release commit), notes in the ignored private evidence folder: 7 of 13 current and observed, 0 stale, 5 gaps, 1 needs a human.

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, and installed and verified on this machine are five facts, each read above. A machine already joined moves with `skilliton pin --latest --apply` and then, until B102 is fixed, `claude plugin install <p>@skilliton --scope project` inside each project that had project-scope installs. Nothing here measures whether the release works on a machine it has not run on.
