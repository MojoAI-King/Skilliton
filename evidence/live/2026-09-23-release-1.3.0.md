# Release 1.3.0, 2026-09-23

Kind: Reference. The commands the release ran, each with its verdict, in the order the release skill gives them. No signer names, no key material. This release carries the fixes from the second review of the day: a regrade of 63b0119 by three fresh reviewers (73 of 100, hire signal HIRE, security B, adversary "need the interview") and a second cold review of the same commit (57 of 80 non-security points), both run the same afternoon (CHANGELOG.md, SECURITY.md).

## Facts before anything was written

- Four lanes dispatched from a plan at a19f791 (gate-review, guard-review-2, runner-review, docs-review-2), each merged on `main` after a rebase and the fast checks on its rebased tree, one at a time, each pushed as it landed, CI read green on every push except one: b90d1e6 carried three red tests for eleven minutes (a hash replacement in the runtime's migration message committed before its tests were read; fixed in fbb4950; docs/lessons/2026-09-23-a-chained-command-commits-before-the-tes-70be.md).
- Two red teams. The gate: first pass found a real gap (the default protected list was read at the pushed tip only, so a symbolic link named `scripts` let a rewritten check resolve elsewhere), fixed in the lane (78842d4, cherry-picked to main as 3930380), second pass 8 of 8 cases as required, 0 open, slowest evaluation 686 ms. The guard: 45 of 46 forms caught, 0 regressions, slowest decision 153 ms; the one open form is `sponge` onto a record file that does not exist yet, which creates an entry rather than emptying one and was left allowed on purpose.
- Full checks on the merged tree 3930380: **81 pass, 0 fail, 2 skipped, of 83** (the two skipped are the CI-only steps; the list grew by two steps and the runner now fails a step the way CI does).
- Versions: workflow 0.23.0 to 0.24.0, guardrails 0.9.0 to 0.10.0; context-hygiene 0.3.1 and code-quality 0.2.1 unchanged.
- The policy change: `.skilliton/delivery.json` gained `protectedPaths` (`scripts/`, `.github/workflows/`) in an approver-signed commit (6741fe0), as the policy-path rule requires.

## Manifest

- `release create --version 1.3.0 --evidence <10 files> --apply` wrote `releases/1.3.0.json`, manifest-sha256 `62dec18badc94462aa614e9d0df6b8620098b1ab891eebe56befae3256191bf8`. Evidence: the same ten files as 1.2.0 with the 1.2.0 release note in place of the 1.1.0 one.
- Committed as 312c212 (signed) and pushed at 18:01 EDT; the handoff commit 6fd624b followed it (records only).
- Whole-history audit on 312c212: 0 findings in 759 changed files, 57 allowed lines carrying a reason.
- Fast checks and `scripts/release.test.mjs` (27 pass) on 312c212, each exit 0.
- CI on 312c212: green (run 35925764172). CI on 3930380, the merged tree: green (run 35925048519).

## Sign, publish, prove

- `release sign 1.3.0 --apply`: the manifest committed and valid, no tag yet, every plugin folder matching; created `skilliton-release/1.3.0` on 6fd624b, signed with SSH; `git tag -v` reads a good signature.
- `git push origin skilliton-release/1.3.0`: new tag on the remote at 18:05 EDT.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **5 approved, 0 unapproved, 0 withdrawn**: 1.3.0 (manifest 62dec18badc9, commit 6fd624b), 1.2.0, 1.1.0, 1.0.0 and 0.9.0.
- On this machine: `claude plugin marketplace update skilliton`, workflow 0.23.0 to 0.24.0 and guardrails 0.9.0 to 0.10.0 at user scope (restart to apply); `skilliton verify`: **all 8 installs VERIFIED** against approved releases (user scope on 1.3.0 for workflow, guardrails and context-hygiene; project scope still pinned at 1.1.0 for workflow and guardrails until a pin; code-quality on 1.0.0 at both).
- The remote after the day's rewrite and three releases: zero co-author lines on `main`, one contributor.

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts; the lines above say which held at the time of writing. The regrade and the second review are the owner's reading and are not evidence of quality; their findings are what this release fixed, and the two left open (`source <file>` asks every time; `sponge` onto a new entry is allowed) are in docs/BACKLOG.md and CHANGELOG.md.
