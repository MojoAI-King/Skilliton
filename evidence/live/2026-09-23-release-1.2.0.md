# Release 1.2.0, 2026-09-23

Kind: Reference. The commands the release ran, each with its verdict, in the order the release skill gives them. No signer names, no key material. This release carries the fixes for the limits three independent cold reviews found the same morning (CHANGELOG.md, docs/THREAT_MODEL.md).

## Facts before anything was written

- Four lanes dispatched from a plan at 029f159 (the commit the reviews read): guard-review, writers-review, docs-review, hygiene-review; each merged on `main` after a rebase and the fast checks on its rebased tree, one lane at a time, each pushed as it landed, CI read green on every push.
- A red team fed the reviews' command lists to the new guard as PreToolUse JSON in a fixture repository: first pass 65 of 65 bypass forms caught, one over-reach (`git push --dry-run -f` denied), fixed; second pass 65 of 65 caught, 0 still allowed, 0 regressions, slowest decision 284 ms.
- Full checks on the merged tree before the release (the commit the version bumps followed): **78 pass, 0 fail, 2 skipped, of 80** (the two skipped are the CI-only steps).
- Versions: workflow 0.22.0 to 0.23.0, guardrails 0.8.0 to 0.9.0; context-hygiene 0.3.1 and code-quality 0.2.1 unchanged.
- The history of `main` was rewritten before the manifest, by the owner from a terminal, to remove 311 AI co-author trailers: two passes (strip, then re-sign, because the branch rule requires signed commits), trees unchanged, the three earlier tags re-signed, force-pushed with the ruleset disabled for the push and re-enabled after (docs/decisions/2026-09-23-the-history-was-rewritten-once-on-2026-0-82b9.md). Zero co-author lines and one contributor on the remote afterwards.

## Manifest

- `release create --version 1.2.0 --evidence <10 files> --apply` wrote `releases/1.2.0.json`, manifest-sha256 `2de2193b9297b695419845b2b9936e09e7cde5a2da631361114b843151ba20cb`. Evidence: the seven files 1.1.0 carried, plus the second comparison summary, the 1.1.0 release note and docs/THREAT_MODEL.md.
- Committed as 63b0119 (signed) and pushed at 12:58 EDT.
- Whole-history audit on 63b0119: 0 findings in 742 changed files, 57 allowed lines carrying a reason.
- CI on 63b0119: green (run 35893765684).
- Full checks locally on 63b0119: **78 pass, 0 fail, 2 skipped, of 80** (run 12:59 to 13:44 EDT, slowed by three review clones running the same suite beside it).

## Sign, publish, prove

- `release sign 1.2.0 --apply`: checked that the manifest is committed and valid, no tag yet, every plugin folder matches; created `skilliton-release/1.2.0` on 63b0119, signed with SSH; `git tag -v` reads a good signature.
- `git push origin skilliton-release/1.2.0`: new tag on the remote at 13:10 EDT.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **4 approved, 0 unapproved, 0 withdrawn**: 1.2.0 (manifest 2de2193b9297, commit 63b0119), 1.1.0, 1.0.0 and 0.9.0 at their re-signed commits.
- On this machine: `claude plugin marketplace update skilliton`, workflow 0.22.0 to 0.23.0 and guardrails 0.8.0 to 0.9.0 at user scope (restart to apply); `skilliton verify`: **all 8 installs VERIFIED** against approved releases (user scope on 1.2.0 for workflow, guardrails and context-hygiene; project scope still pinned at 1.1.0 for workflow and guardrails until a pin; code-quality on 1.0.0 at both).

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts; the lines above say which held at the time of writing. The self-assessment that followed (three fresh reviewers grading the tag from the rubric alone) is the owner's to read and is not evidence of quality; it is in the session's records, not here.
