# Release 1.4.3, 2026-09-24

Kind: Reference. The commands the release ran, each with its verdict, in the order the release skill gives them. No signer names, no key material. This release carries the guard's quiet mode (guardrails 0.12.0), built in lane/quiet-guard-0924 from the owner's rule of 2026-09-24 that nothing asks during work, and the managed block's sentence about it (workflow 0.25.3). It is numbered 1.4.3 at the owner's request; the release skill's rule would call a new guard behavior a minor release.

## Facts before anything was written

- The lane: five commits on f605f64, a clean worktree. On the rebased tree, with stdin closed: guardrails.test.sh 670, bypass 144, review 224, review2 163, timing 16, modes 61, all pass; the modes suite also passes 61 under a pseudo-terminal (`script -q /dev/null`, a 120 s alarm). Merged fast-forward only.
- Two defects the lane's own suites could not see, caught by the full check list at merge and fixed on main before the release: `scripts/guardrails.test.sh` had grown past its size pin (1,018 lines against 1,015; the strict setting now sits in the suite's environment export, 1,015 lines, 670 pass), and `scripts/guardrails-modes.test.sh` was not executable.
- The main-only half: docs/CONTRACTS.md (`guardrails.mode`, the notes file and its four fields, `SKILLITON_GUARDRAILS_MODE`, the template size), README, INSTALL and SECURITY.md (a command the hook cannot read now runs and is noted), the harness template's guard sentence with migration 0100-instructions-3ced7e976ae9 applied here, the lane's decision and two lessons accepted and indexed, and a CI step for the modes suite. `dispatch close --apply` closed the lane's task as merged; it read the lane as 0 requests because its session was opened on another folder (backlog B93).
- Changed since 1.4.2: guardrails 0.11.0 to 0.12.0 and workflow 0.25.2 to 0.25.3; context-hygiene 0.3.1 and code-quality 0.2.1 have no changed file.

## Manifest

- `release create --version 1.4.3 --evidence <17 files> --apply` wrote `releases/1.4.3.json`, manifest-sha256 `27edff7df2d37b4fa15e40a041de7d2249681f1c08704ec5bf6b5488538fdaf1`. Evidence: the sixteen files of 1.4.2 with the 1.4.2 release note in place of the 1.4.1 one, plus the quiet-mode decision entry.
- Committed as c964483 after the notes commit 45bbde6, pushed.
- Full checks on c964483, the manifest commit: **114 pass, 0 fail, 2 skipped, of 116**, exit 0 (the two skipped are CI-only), with the runner holding the Mac awake.
- A slip before signing: a task checkpoint was committed on top of c964483 (6ae2b20, one task record changed, nothing under a plugin folder), and `release sign`, which tags HEAD, put the tag on 6ae2b20. The tag was held back and the full list run again on 6ae2b20: **114 pass, 0 fail, 2 skipped, of 116**, exit 0. Backlog B96 asks for sign to name commits that follow the manifest commit.
- CI on c964483 and on 6ae2b20: green.

## Sign, publish, prove

- `release sign 1.4.3 --apply`: the manifest committed and valid, no tag yet, every plugin folder matching; created `skilliton-release/1.4.3` on 6ae2b20, signed with SSH; `git tag -v` reads a good signature carrying manifest-sha256 27edff7df2d3.
- `git push origin skilliton-release/1.4.3` after the second full run: new tag on the remote at 00:07 EDT on 2026-09-25.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **9 approved, 0 unapproved, 0 withdrawn**, 1.4.3 with manifest 27edff7df2d3 on commit 6ae2b2084a81.
- On this machine: `claude plugin marketplace update skilliton` from a neutral folder, then `claude plugin update` for workflow (0.25.2 to 0.25.3) and guardrails (0.10.0 to 0.12.0) at user scope, and from the checkout at project scope (workflow 0.25.2 to 0.25.3, guardrails 0.8.0 to 0.12.0). The guardrails update replaced the copies another session had edited in place. The repository's `.claude/settings.json` was byte-identical before and after (sha256 prefix 25c37949ecdd). Restart to apply.
- `skilliton verify`: **8 VERIFIED, 0 TAMPERED**: every company plugin at both scopes matches an approved release (workflow 0.25.3, guardrails 0.12.0 and context-hygiene 0.3.1 on 1.4.3, code-quality 0.2.1 on 1.0.0). The first time on 2026-09-24 that every copy on this machine is a signed release.
- The older-reader check: 0.25.2 and 0.25.3 both read the security register with 0 invalid records and the same six stale controls, which were then reassessed (7 of 13 current and observed, 0 stale, 5 gaps, 1 needs a human).

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts. At the time of writing all five held for every plugin on this machine.
