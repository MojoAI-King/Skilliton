# Release 1.0.0, signed, pushed and verified, 2026-09-22

Kind: Reference (evidence). Cut at 17:55 to 18:05 EDT on the maintainer's machine through `/workflow:release`, on the owner's go in the conversation.

## What ran, in order

1. Preconditions at commit 9ff92ef: `main`, clean, level with `origin/main`; `release list` showed 0.9.0 approved and trust configured; every changed plugin carried a new version and context-hygiene 0.3.0 kept the same tree hash as in 0.9.0; the changelog named every changed plugin; no migration pending; `scripts/checks.mjs` 56 pass, 0 fail, 2 skipped, and the hosted run 64 steps with none failed. Two attention lines were accepted by the owner: the 15 undecided security controls, and a handoff that the release checkpoint rewrites.
2. `skilliton release create --version 1.0.0 --evidence <seven files> --apply`: `releases/1.0.0.json`, manifest-sha256 `a34e53607277...`. The changelog's Unreleased entries moved under `## 1.0.0, 2026-09-22`. `node --test scripts/release.test.mjs` 27 of 27, docs and scrub checks passed. Committed as `Release 1.0.0` (e5cc2c6).
3. `skilliton release sign 1.0.0 --apply`: tag `skilliton-release/1.0.0` on e5cc2c6, signed with SSH. `release list` showed it approved with the same manifest hash.
4. `git push origin main` and `git push origin skilliton-release/1.0.0`.
5. A fresh clone from GitHub into a temporary folder: `release list --repo <that folder>` showed 1.0.0 approved, manifest-sha256 `a34e53607277`, commit e5cc2c6.
6. `claude plugin marketplace update skilliton`, then `skilliton verify`: VERIFIED for workflow 0.21.0, guardrails 0.7.0, context-hygiene 0.3.0 and code-quality 0.2.1, at user and project scope, 8 of 8, "all files match".

## Not measured here

A machine other than this one. A developer machine already joined moves to this release with `skilliton pin --latest --apply` and its client's plugin update; that was not run anywhere else. The owner's keyboard tests in docs/OWNER_TESTS.md ran after this release, not before it, by the owner's choice.

Note, 2026-09-23: the commit hashes in this note were rewritten on 2026-09-23 when the co-author trailers were removed from history; the trees are unchanged and the tags were re-signed at the rewritten commits (docs/decisions/2026-09-23-the-history-was-rewritten-once-on-2026-0-82b9.md).
