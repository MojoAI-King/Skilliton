# Release 1.4.0, 2026-09-24

Kind: Reference. The commands the release ran, each with its verdict, in the order the release skill gives them. No signer names, no key material. This release carries the batch the owner asked for on 2026-09-23 (finish everything a session can: the second field report's usage asks, the open backlog rows, the security records) in the order an outside review suggested: claims beside their limits, an independent newcomer walkthrough, the friction it found, then security verification.

## Facts before anything was written

- Seven lanes dispatched from three plans (LANES-8 at b1fc441, LANES-8b, LANES-8c): usage, security-records, hooks-and-tasks, guard, pin-at-tag, friction, legacy-and-template. Each merged on `main` fast-forward only after a rebase, the fast checks (10 of 10), the lane's own suites, the demonstration and the project rehearsal on its rebased tree, one at a time, each pushed as it landed. CI went red once, on the demonstration after the security-records merge (fixed in 90da07f); the demonstration and the rehearsal have been in every merge check since (docs/lessons/2026-09-24-a-merge-check-that-leaves-out-the-demo-a-76b8.md). One lane item (N9, the guard's tokenizer in Node) was measured and not merged by decision.
- A pre-release review of the security records' changed sources found and fixed, each reproduced first and each with a test that fails against the code before it: `harness --apply`, `project-settings --apply` and `dispatch merge` wrote through a committed symbolic link outside the repository (the same code is in 0.9.0 and 1.3.0; SECURITY.md lists it); the new usage ledger did the same in its first version; the gate verdict printed other processes' command lines; a security record could not cite a lesson or decision entry.
- Security records: nine reassessments for the eight controls whose fingerprinted files changed, and SG-SECURITY-TESTS after the full run below. `security status`: 7 of 13 applicable controls current and observed, 0 stale, 5 gaps and 1 that needs a human, each in docs/SECURITY_FINDINGS.md.
- Full checks on 4e3c697 (the tree before the records and the manifest): **106 pass, 0 fail, 2 skipped, of 108** (the two skipped are CI-only steps).
- Versions: workflow 0.24.0 to 0.25.0, guardrails 0.10.0 to 0.11.0; context-hygiene 0.3.1 and code-quality 0.2.1 unchanged (no file changed under either since 1.3.0).
- Migration 0100-instructions-db85286c7260 applied to this repository (the smaller block), receipt committed; `migrate` and `prepare --check` report nothing pending.

## Manifest

- `release create --version 1.4.0 --evidence <13 files> --apply` wrote `releases/1.4.0.json`, manifest-sha256 `8b70c7524e98b80ae1f3375e26aa0f3c03f34f863b7eea50e3c06471888465d5`. Evidence: the ten files of 1.3.0 with the 1.3.0 release note in place of the 1.2.0 one, plus the second field report, the marketplace pin measurement and the newcomer walkthrough.
- Committed as 4bc1c2c (signed) with the changelog and pushed at 16:10 EDT.
- Full checks on 4bc1c2c, the manifest commit, before signing: **106 pass, 0 fail, 2 skipped, of 108**, exit 0. `scripts/release.test.mjs` 27 pass on the same tree.
- CI on 4bc1c2c: green (run 36053099223); every push of the batch before it read green, after the one red on the demonstration named above.

## Sign, publish, prove

- `release sign 1.4.0 --apply`: the manifest committed and valid, no tag yet, every plugin folder matching; created `skilliton-release/1.4.0` on 4bc1c2c, signed with SSH; `git tag -v` reads a good signature.
- `git push origin skilliton-release/1.4.0`: new tag on the remote at 17:04 EDT.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **6 approved, 0 unapproved, 0 withdrawn**, 1.4.0 with manifest 8b70c7524e98 on commit 4bc1c2c13fe1.
- On this machine, from a neutral temporary folder: `claude plugin marketplace update skilliton` (the marketplace tracks `main`, which was exactly the release commit), then `claude plugin update workflow@skilliton`, 0.24.0 to 0.25.0 at user scope (restart to apply). Guardrails was **not** updated here on purpose: another session is building a guard mode that refuses instead of asking, and it has edited `hooks/guard-bash.sh` in the installed copies in place; installing 0.11.0 would replace that edit on this machine.
- `skilliton verify`: **6 VERIFIED, 2 TAMPERED**. VERIFIED: context-hygiene 0.3.1 at both scopes and workflow 0.25.0 at user scope on 1.4.0, workflow 0.22.0 at project scope on 1.1.0 (pinned there until a pin), code-quality 0.2.1 at both scopes on 1.0.0. TAMPERED: guardrails 0.10.0 at user scope and 0.8.0 at project scope, each with one changed file, `hooks/guard-bash.sh`: the in-place edit above, which verify is built to catch. This machine is therefore not VERIFIED as a whole until that work is released and installed from an approved release.

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts. At the time of writing the first four held; on this machine workflow 0.25.0 is installed and VERIFIED, and guardrails is not on 1.4.0 and reads TAMPERED, for the reason above.
