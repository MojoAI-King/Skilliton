# Release 1.1.0, 2026-09-23

Kind: Reference. The commands a release runs, each with its verdict, in the order the release skill gives them. No signer names, no key material.

## Facts before anything was written

- Branch `main`, working tree clean, `main...origin/main` ahead only (5 unpushed commits, none behind) before the manifest.
- `release list --repo .`: trust configured for one company, 1.0.0 and 0.9.0 approved.
- Version 1.1.0, not the planned 1.0.1: the release skill's step 5 says minor for a new command, rule or gate behavior, and this release carries `security applicability --propose`, the maintain refresh step, the gate's machine context, the shape rules and an instruction migration (decision 2026-09-23-the-night-s-release-is-1-1-0-not-1-0-1-b-46b5).
- Every changed plugin has a new version since 1.0.0: workflow 0.22.0, guardrails 0.8.0, context-hygiene 0.3.1, code-quality 0.2.1 (the preview listed all four with their tree hashes).
- `status`: no pending migration (0100-instructions-d00c8352985c applied here in 6dd059b), attention only on the handoff age and the security register (the register is the owner's applicability decisions; its records are collected below).
- Full checks on the pre-manifest tree 6dd059b: **76 pass, 0 fail, 2 skipped, of 78** (the two skipped are the CI-only steps).

## Manifest

- Attribution commit 964e9cb first (the licence, marketplace owner and plugin author fields).
- `release create --version 1.1.0 --repo . --evidence <7 files> --apply` wrote `releases/1.1.0.json`, manifest-sha256 `02f692d9a0c0431100f34f50ad10fa46ee72c7f73a2edb0423efb312408afef2`. Evidence: the client field report, the Windows hosted-runner port note, the VS Code extension hooks note, the Codex session note, the use-on-this-machine count, the report card, the first comparison's summary.
- The changelog's sixteen Unreleased entries moved under `## 1.1.0, 2026-09-23`; committed together as 8cd1779.
- Whole-history audit on 8cd1779: 0 findings in 702 changed files, 51 allowed lines carrying a reason.
- Full checks again on 8cd1779: **76 pass, 0 fail, 2 skipped, of 78** (run 09:44 to 09:55 EDT).

## Sign, publish, prove

- `release sign 1.1.0 --repo .` preview: the manifest is committed and valid, no tag yet, every plugin folder matches the manifest.
- `release sign 1.1.0 --repo . --apply`: created `skilliton-release/1.1.0` on commit 8cd1779, signed with SSH (the tag object carries an SSH signature block).
- `git push origin main` and `git push origin skilliton-release/1.1.0`: both pushed at 09:56 EDT; `main...origin/main` level.
- Fresh clone (`git clone` into a temporary folder this checkout did not make), `release list --repo <clone>`: the clone's HEAD is 8cd1779 and it lists **1.1.0 approved**, manifest-sha256 02f692d9a0c0, commit 8cd1779, beside 1.0.0 and 0.9.0.
- On this machine: `claude plugin marketplace update skilliton`, then the plugin updates, then `skilliton verify`: the marketplace updated; workflow 0.21.0 to 0.22.0, guardrails 0.7.0 to 0.8.0, context-hygiene 0.3.0 to 0.3.1 at user scope (code-quality already 0.2.1), then the same at project scope; `skilliton verify` reads **VERIFIED for all 8 installs** (release 1.1.0 for the updated ones, every file matching). Restart the client before the new hooks run.

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts; the lines above say which held at the time of writing.

Note, 2026-09-23: the commit hashes in this note were rewritten on 2026-09-23 when the co-author trailers were removed from history; the trees are unchanged and the tags were re-signed at the rewritten commits (docs/decisions/2026-09-23-the-history-was-rewritten-once-on-2026-0-82b9.md).
