# Release 1.4.1, 2026-09-24

Kind: Reference. The commands the release ran, each with its verdict, in the order the release skill gives them. No signer names, no key material. This is a patch release of one plugin: workflow 0.25.1, which carries the fix for dispatch writing through a committed link and the corrections the first eval run after 1.4.0 led to.

## Facts before anything was written

- Branch `main`, clean, in step with `origin/main`. `release list` before: 6 approved (0.9.0 to 1.4.0), 0 unapproved, 0 withdrawn.
- Changed since 1.4.0: workflow only, 16 files, bumped 0.25.0 to 0.25.1; guardrails 0.11.0, context-hygiene 0.3.1 and code-quality 0.2.1 have no changed file.
- The security retest found that `dispatch --apply` wrote a lane's brief through a committed symbolic link outside the repository, and B88's list of every file write found four more writers with no link check (`company init`, `new-plugin`, `release create`, the delivery policy confirm). Each was reproduced first and fixed with a test that fails against the code before it (`scripts/dispatch-links.test.mjs`, `scripts/write-sites.test.mjs`); SECURITY.md and docs/THREAT_MODEL.md name it.
- The eval of `audit-finding-acted-on` on 1.4.0 read 0.62 with the plugin and 0.86 without; from the traces every run fixed the flaw, and the difference came from the security skill and three graders (docs/lessons/2026-09-24-an-eval-difference-came-from-graders-one-0135.md). After the fixes in this release: 1.00 and 1.00 over six runs (evidence/60aed2c/audit-finding-acted-on/SUMMARY.md).
- Found while preparing it: workflow 0.24.0 and earlier read this repository's security register as 13 of 13 controls invalid, because two current records cite a lesson entry name the old path rule refused; 0.25.0 and 0.25.1 read 0 invalid. It surfaced because this machine's project-scope workflow was still 0.22.0 (release 1.1.0). CHANGELOG.md 1.4.1 carries it as an upgrade note.

## Manifest

- `release create --version 1.4.1 --evidence <16 files> --apply` wrote `releases/1.4.1.json`, manifest-sha256 `a410ec8475d4493667a0c8f6ff453dd0d84586f2743853fd790f115cd8dbec3b`. Evidence: the thirteen files of 1.4.0 with the 1.4.0 release note in place of the 1.3.0 one, plus the third comparison run and both eval summaries.
- Committed as 52b24f8 after the notes commit 4fa77e3, pushed.
- Full checks on 52b24f8, the manifest commit, before signing: **110 pass, 0 fail, 2 skipped, of 112**, exit 0 (the two skipped are CI-only steps). That was the second run. The first, started at 18:27, failed two steps whose tests took 1,039 s and 968 s (machine setup with join and undo; verify naming a pinned clone): the Mac went into system sleep at 19:07 and again at 19:25 (pmset -g log), each test passed alone in seconds, and the run was stopped and started again under `caffeinate -ims`, with no sleep logged while it ran. Why each command failed across the sleep is not known, because neither test prints the command's own message; backlog B91.
- CI on 52b24f8: green (run 36068907569), as were the three pushes before it.

## Sign, publish, prove

- `release sign 1.4.1 --apply`: the manifest committed and valid, no tag yet, every plugin folder matching; created `skilliton-release/1.4.1` on 52b24f8, signed with SSH; `git tag -v` reads a good signature carrying manifest-sha256 a410ec8475d4.
- `git push origin skilliton-release/1.4.1`: new tag on the remote at 19:52 EDT.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **7 approved, 0 unapproved, 0 withdrawn**, 1.4.1 with manifest a410ec8475d4 on commit 52b24f84051c.
- On this machine, from a neutral temporary folder: `claude plugin marketplace update skilliton` (the marketplace tracks `main`, which was exactly the release commit), then `claude plugin update workflow@skilliton`, 0.25.0 to 0.25.1 at user scope, and from the checkout `claude plugin update workflow@skilliton --scope project`, 0.22.0 (release 1.1.0) to 0.25.1; the repository's `.claude/settings.json` was byte-identical before and after (sha256 prefix 25c37949ecdd). Restart to apply. Guardrails was **not** updated, for the same reason as in 1.4.0: the other session's in-place edit of `hooks/guard-bash.sh`.
- `skilliton verify`: **6 VERIFIED, 2 TAMPERED**. VERIFIED: workflow 0.25.1 at both scopes on 1.4.1, context-hygiene 0.3.1 at both scopes on 1.4.1, code-quality 0.2.1 at both scopes on 1.0.0. TAMPERED: guardrails 0.10.0 (user) and 0.8.0 (project), `hooks/guard-bash.sh` changed, the edit named above.
- The older-reader check (the upgrade note in CHANGELOG.md 1.4.1): the installed 0.25.1 copy reads the security register with 0 invalid records, where the project-scope 0.22.0 copy it replaced read 13 of 13 controls invalid.

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts. At the time of writing all five held for workflow at both scopes; guardrails is not on 1.4.1 on this machine and reads TAMPERED, for the reason above.
