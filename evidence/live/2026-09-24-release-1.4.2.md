# Release 1.4.2, 2026-09-24

Kind: Reference. The commands the release ran, each with its verdict, in the order the release skill gives them. No signer names, no key material. A patch release of one plugin, workflow 0.25.2, with three fixes: `dispatch close` counts a lane that ran as an agent (B89), a check run keeps a Mac awake and names a sleep in a failing step (B91), and a refused security record names the entry and the rule (B92).

## Facts before anything was written

- Branch `main`, clean apart from one untracked generated report (`.skilliton/security/REPORT.md`, written by `security status --apply` and never committed), in step with `origin/main`. `release list` before: 7 approved (0.9.0 to 1.4.1), 0 unapproved, 0 withdrawn.
- Changed since 1.4.1: workflow only, bumped 0.25.1 to 0.25.2; guardrails 0.11.0, context-hygiene 0.3.1 and code-quality 0.2.1 have no changed file.
- The three fixes were built on two branches (lane/lane-cost-0924, lane/record-refusal-0924), each commit with a test that fails against 1.4.1: scripts/lane-agent-cost.test.mjs (6), scripts/awake.test.mjs (6), scripts/security-path-refusals.test.mjs (5). The full run on the rebased first branch (e7df4a8): **112 pass, 0 fail, 2 skipped, of 114**, the first run that held the Mac awake by itself (`pmset -g assertions` listed caffeinate asserting for the runner's own process). Both branches merged fast-forward only.
- Measured with the new meter option on this machine: three lanes of the 2026-09-24 batch that `dispatch close` read as 0 requests read 197, 97 and 164, with peak contexts of 493,049, 202,727 and 287,464 tokens against the 200,000 bound.

## Manifest

- `release create --version 1.4.2 --evidence <16 files> --apply` wrote `releases/1.4.2.json`, manifest-sha256 `d418b32e19589345c1513244eee623f8e69b64813143afdaf1a81d3be08a4ee2`. Evidence: the sixteen files of 1.4.1 with the 1.4.1 release note in place of the 1.4.0 one.
- Committed as 20b410c after the notes commit decf46e, pushed.
- Full checks on 20b410c, the manifest commit, before signing: **113 pass, 0 fail, 2 skipped, of 115**, exit 0 (the two skipped are CI-only steps). The runner printed `checks: kept awake: caffeinate holds this Mac awake until the run ends` on its first line.
- CI on 20b410c: green, as on every push since 1.4.1.

## Sign, publish, prove

- `release sign 1.4.2 --apply`: the manifest committed and valid, no tag yet, every plugin folder matching; created `skilliton-release/1.4.2` on 20b410c, signed with SSH; `git tag -v` reads a good signature carrying manifest-sha256 d418b32e1958.
- `git push origin skilliton-release/1.4.2`: new tag on the remote at 21:03 EDT.
- Fresh clone from GitHub into a folder this checkout did not make, checked out at the tag: `release list --repo <clone>` lists **8 approved, 0 unapproved, 0 withdrawn**, 1.4.2 with manifest d418b32e1958 on commit 20b410c7e7ce.
- On this machine: `claude plugin marketplace update skilliton` from a neutral folder, then `claude plugin update workflow@skilliton` 0.25.1 to 0.25.2 at user scope, and from the checkout `--scope project` 0.25.1 to 0.25.2; the repository's `.claude/settings.json` was byte-identical before and after (sha256 prefix 25c37949ecdd). Restart to apply. Guardrails not updated, for the reason in the 1.4.0 and 1.4.1 notes.
- `skilliton verify`: **6 VERIFIED, 2 TAMPERED**. VERIFIED: workflow 0.25.2 at both scopes and context-hygiene 0.3.1 at both scopes on 1.4.2, code-quality 0.2.1 at both scopes on 1.0.0. TAMPERED: guardrails 0.10.0 (user) and 0.8.0 (project), `hooks/guard-bash.sh` changed by the other session's in-place edit.
- The older-reader check (the upgrade note in CHANGELOG.md 1.4.1): the previous release's installed copy (0.25.1) and this one (0.25.2) both read the security register with 0 invalid records and the same four stale controls; the four were then reassessed (7 of 13 current and observed, 0 stale, 5 gaps, 1 needs a human).

## What each state means

Manifest committed, tag signed, pushed, approved in a fresh clone, installed and verified on this machine are five different facts. At the time of writing all five held for workflow at both scopes; guardrails is not on 1.4.2 on this machine and reads TAMPERED, for the reason above.
