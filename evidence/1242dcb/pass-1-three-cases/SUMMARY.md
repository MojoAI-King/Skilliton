# Eval evidence for 1242dcbc915f

Kind: Reference. Generated by `scripts/evidence.mjs` from a `claude plugin eval` run; do not edit by hand.

- Commit: `1242dcbc915fcdc93a071a473a45abc21cf155b6`
- Plugins: `code-quality` 0.1.0
- Claude Code: 2.1.276; started 2026-09-21T05:17:22.911Z; 439 s; ablation with-without; partial: false
- Cost: about $9.29, the eval tool's list-price estimate for running this eval (not plan usage, not a savings figure)
- Raw result sha256: `91a14aa9963308d9a0b88b9ef9b896233897577a29a303f8fa78015b9ca848a2`

Overall score with the plugin: 0.93; mean difference with and without: 0.03.

| Case | With plugin | Difference | Runs per arm |
|---|---|---|---|
| `dead-code-with-a-live-caller` | 0.86 | 0.05 | 3 |
| `one-concept-three-names` | 0.93 | -0.07 | 3 |
| `split-keeps-behavior` | 1.00 | 0.11 | 3 |

## Checks per case

Counts are runs passed out of runs. Indicator checks (whether the skill fired) are shown but not scored.

### `dead-code-with-a-live-caller`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| channel-list-intact | regex | 3 of 3 | 3 of 3 |
| dead-helper-removed | regex | 3 of 3 | 3 of 3 |
| live-handler-kept | file_exists | 0 of 3 | 0 of 3 |
| live-helper-kept | regex | 3 of 3 | 3 of 3 |
| searched-beyond-the-source | llm | 3 of 3 | 2 of 3 |
| second-dead-helper-removed | regex | 3 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |
| unproven-named-as-unproven | llm | 3 of 3 | 3 of 3 |

### `one-concept-three-names`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| boundary-explained | llm | 3 of 3 | 3 of 3 |
| config-key-renamed | regex | 3 of 3 | 3 of 3 |
| old-name-gone-from-billing | regex | 3 of 3 | 3 of 3 |
| old-name-gone-from-store | regex | 3 of 3 | 3 of 3 |
| old-names-banned | regex | 2 of 3 | 3 of 3 |
| ran-the-checks | tool_used | 3 of 3 | 3 of 3 |
| readme-updated | regex | 2 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |
| vendor-field-kept | regex | 3 of 3 | 3 of 3 |
| vendor-shape-unchanged | regex | 3 of 3 | 3 of 3 |

### `split-keeps-behavior`

| Check | Type | With plugin | Without plugin |
|---|---|---|---|
| boundary-test-intact | regex | 3 of 3 | 3 of 3 |
| moved-not-rewritten | llm | 3 of 3 | 1 of 3 |
| pin-not-raised | regex | 3 of 3 | 3 of 3 |
| ran-the-lint | tool_used | 3 of 3 | 3 of 3 |
| ran-the-tests | tool_used | 3 of 3 | 3 of 3 |
| skill-fired (indicator) | tool_used | 3 of 3 | not run |
| wrote-a-new-module | tool_used | 3 of 3 | 3 of 3 |

## Notes

Pass 1 of three. Run 2026-09-21, judge model sonnet, three runs per arm.

Command, from the repository root:

    "$CLAUDE_CODE_EXECPATH" plugin eval packs/base/plugins/code-quality --trust-plugin --no-publish \
      --scaffold -j 4 --output-dir <folder outside the repository> --keep-temp --judge-model sonnet \
      --max-cost-usd 12 --allow-tools Bash Write Edit

Run with the editor's own client, 2.1.276. The client that PATH resolves on this machine is 2.1.92 and
does not accept every option used here.

Two results in this file are known to be wrong, and both were diagnosed rather than left standing:

1. `a-test-that-cannot-fail` is absent. It never ran: its prompt.md description contained an unquoted
   ": ", which YAML reads as a nested mapping, so the frontmatter was dropped and the case was refused.
   The tool reported this on the run's second line and again in its last line. Pass 2 is that case after
   the fix.

2. `dead-code-with-a-live-caller` reads with 0.86, without 0.81, delta +0.05. That delta is not a result.
   Its trap grader `live-handler-kept` was a `file_exists` check asking whether a file had survived, which
   that grader type cannot answer: it reads the run's own file operations, not the workspace, so a file
   the fixture wrote and the run correctly left alone reads as missing. It failed 0/3 in both arms while
   the file was present in all six kept scaffold trees. Pass 3 is that case re-run with the grader
   rewritten as a regex over the file. Commit 1242dcb has the mechanism and the three probe cases that
   measured it.

The two rows that stand are `split-keeps-behavior` (with 1.00, without 0.89, delta +0.11; the
discriminating grader is `moved-not-rewritten`, 3/3 with against 1/3 without) and
`one-concept-three-names` (with 0.93, without 1.00, delta -0.07; the without arm was 3/3 perfect and the
with arm lost one grader in each of two runs, a different grader each time, which is noise at a ceiling
rather than evidence the skill harms).

The commit this is bound to carries the case definitions as corrected. The dead-code case as it ran in
this pass had the broken grader; that is why its row here is superseded by pass 3 and not by an
amendment to this file.
